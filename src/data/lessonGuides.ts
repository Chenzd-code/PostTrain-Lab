import type { ChapterGuide, CodeWalkthrough, LearningVisual, LessonGuide } from '../types'

const code = (
  title: string,
  language: CodeWalkthrough['language'],
  lines: string[],
  notes: string[],
): CodeWalkthrough => ({ title, language, code: lines.join('\n'), notes })

const guide = (
  plain: string,
  analogy: string,
  why: string,
  steps: Array<[string, string]>,
  walkthrough: CodeWalkthrough,
  visual?: LearningVisual,
): LessonGuide => ({
  plain,
  analogy,
  why,
  steps: steps.map(([title, detail]) => ({ title, detail })),
  code: walkthrough,
  visual,
})

export const chapterGuides: Record<string, ChapterGuide> = {
  sft: {
    plainDefinition: 'SFT 就是给模型看大量“问题和理想回答”，让它学会像一个合格助手那样接话、遵循格式并完成任务。',
    roleInPipeline: '它通常是后训练的第一站：先把基座模型变成可用助手，后续偏好优化和强化学习才有稳定起点。',
    learningArc: ['先理解模型到底在模仿什么', '再学会制作和检查训练数据', '最后处理训练效率、多轮工具和长上下文'],
  },
  preference: {
    plainDefinition: '偏好阶段不再只告诉模型“标准答案是什么”，而是告诉它“两个回答里哪个更好，以及好在哪里”。',
    roleInPipeline: '它把正确性、帮助性、安全和风格等人类要求，变成奖励模型或偏好优化可以读取的训练信号。',
    learningArc: ['先学会可靠地比较回答', '再把比较结果拟合成奖励', '最后处理多目标冲突和奖励不确定性'],
  },
  rlhf: {
    plainDefinition: 'RLHF 让模型自己尝试回答，再根据奖励判断这次尝试好不好，并提高好动作再次出现的概率。',
    roleInPipeline: '它补上 SFT 只能模仿示范、难以主动探索的短板，但也引入在线采样、价值估计和训练稳定性成本。',
    learningArc: ['先看清四个模型如何协作', '再拆解策略梯度、PPO、GAE 和 KL', '最后学习崩溃诊断与现代基线'],
  },
  'direct-preference': {
    plainDefinition: 'DPO 把“更喜欢 A、不喜欢 B”直接写进语言模型的训练损失，不必先训练奖励模型再跑 PPO。',
    roleInPipeline: '它是偏好对齐的低复杂度起点，适合已有离线偏好对、暂时不需要在线探索的场景。',
    learningArc: ['从直觉理解偏好间隔', '再比较不同 DPO 变体', '最后掌握数据边界和实现排错'],
  },
  'reasoning-rl': {
    plainDefinition: '推理 RL 让模型多次尝试数学、代码或结构化任务，用可验证结果奖励真正做对的答案。',
    roleInPipeline: '可靠验证器提供了比主观打分更硬的反馈，使模型有机会通过探索找到示范数据之外的新解法。',
    learningArc: ['先理解可验证奖励', '再学习 GRPO 的组内比较', '最后处理长推理、过程奖励和现代算法差异'],
  },
  'agent-foundations': {
    plainDefinition: '这一章从最容易被忽略的事实开始：语言模型本身只生成 token，它不能直接打开文件、调用数据库或替用户付款。Agent 是一套受控闭环，模型依据当前观察提出下一步意图，外部运行时验证并执行工具，再把真实结果交回模型。你会先看懂最底层的模型调用协议，再逐步搭出 loop、工具、状态、记忆和多 Agent，而不是一上来背框架名词。',
    roleInPipeline: '在后训练路线里，Agent 基础把“会生成回答的模型”接到“会改变环境的系统”。这也是 Tool SFT、Agent Evals 和 Agentic RL 的共同前置知识：训练数据中的 tool call、线上协议中的 call/result、强化学习里的 action/observation 必须使用一致语义。',
    learningArc: ['先建立模型、工具、Agent 与 Harness 的责任边界', '再读懂无状态 API、消息、工具调用和流式事件', '随后手写带五道闸门的 Agent Loop', '最后学习工具、状态、记忆与上下文隔离式编排'],
    prerequisites: ['理解一次普通 LLM 推理是“输入 token → 输出 token”', '知道 JSON 和函数参数的基本形状即可，不要求先会任何 Agent 框架'],
    readingAdvice: '先按顺序完成前 3 课并动手跟一遍代码；后面的工具、记忆和多 Agent 都是在这个最小闭环上增加责任。遇到框架术语时，始终追问：它负责组装上下文、执行副作用，还是只是在描述模型意图？',
  },
  'agent-harness': {
    plainDefinition: 'Harness 可以把它理解为 Agent 的控制面：模型负责提出“下一步想做什么”，Harness 负责决定它此刻看见什么、有哪些工具、请求是否获准、动作在哪个沙箱执行、失败怎样恢复、什么时候停以及整条轨迹如何评测。本章按任务开始前、运行中和运行后三种时间尺度展开九大组件，并把参考知识库的 13 个深挖问题嵌入对应课程。',
    roleInPipeline: 'Harness 不等于某个 Agent SDK，也不等于一段 ReAct loop。它把概率模型连接到确定性的权限、状态机、进程、工具和验证器，是 Agent SFT 线上协议、Agentic RL 环境接口和生产可靠性的共同底座。许多看似“模型不够聪明”的问题，实际应通过工具、上下文、错误协议或终止条件解决。',
    learningArc: ['先用三种时间尺度和九组件建立全景地图', '按契约、工具、上下文与回收策略搭建输入和动作面', '再用 Policy/Sandbox、恢复、终止与人工控制真实副作用', '最后学习编排、Tracing/Eval 以及 Tool/MCP/Skill 的完整组合'],
    prerequisites: ['先完成 Agent 基础中的模型协议与最小 Loop', '能区分“模型提出 tool call”和“运行时实际执行工具”', '知道上下文窗口有限，且外部工具可能失败或返回不可信内容'],
    readingAdvice: '严格按课程顺序阅读，并在每一课回答两个问题：这一层防的是哪种真实失败？它的决定是否由代码、模型还是操作系统执行？组件不是越多越好；学完后应能为简单任务删掉不必要的控制，也能为高风险长任务补齐缺口。',
  },
  'agentic-rl': {
    plainDefinition: 'Agentic RL 不是让模型输出一个更高分的单轮回答，而是让它在多步环境里学习：先观察什么、调用哪个工具、参数怎样填写、看到失败后如何调整，以及何时停止。训练样本因此从一段 response 变成包含 observation、action、reward 和终止原因的 trajectory。',
    roleInPipeline: '它位于强 Agent SFT、稳定 Harness 与可验证环境之后。Harness 固定动作如何执行和记录，环境提供可重置的世界与 verifier，RL 才负责在这些边界内改进策略。若三者没有分开，reward 提升既可能来自模型学习，也可能只是工具、上下文或环境漏洞。',
    learningArc: ['先判断问题该改 Harness 还是训练 policy', '再用 POMDP 明确 observation/action/return', '随后建设可重置环境和独立 verifier', '最后处理长程信用分配、异步 rollout 与 policy lag'],
    prerequisites: ['掌握 SFT、策略梯度与 on-policy/off-policy 的基本区别', '完成 Agent 基础和 Harness 章节，能读懂完整 tool trajectory', '能够为目标任务写出不依赖模型自报的成功验证器'],
    readingAdvice: '不要从算法名开始。先为一个小任务实现 reset/step/verifier 和强 baseline，再观察失败是否真的需要探索；每次训练都记录 policy、Harness、environment、reward 四类版本，并按最终成功、成本和安全共同评估。',
  },
  evaluation: {
    plainDefinition: '评测就是在训练前后用固定尺子检查：模型具体哪里变好了、哪里退化了、结论是否可信。',
    roleInPipeline: '没有分层评测，训练指标只是内部信号；是否上线必须由能力、安全、轨迹和真实业务指标共同决定。',
    learningArc: ['先建立四层评测', '再诊断 Agent 轨迹和安全边界', '最后做因果实验与在线持续评测'],
  },
  'training-systems': {
    plainDefinition: '训练系统要把生成、打分、反向传播和权重同步安排到多张 GPU 上，让数据持续流动而不堵车。',
    roleInPipeline: '算法决定要算什么，系统决定能否在合理时间、成本和偏差范围内算完。',
    learningArc: ['先做显存账本', '再拆 rollout 与权重同步', '最后学习异步流水线、容错和成本归因'],
  },
  distillation: {
    plainDefinition: '蒸馏就是让较小或较便宜的学生模型，学习强教师、搜索过程或 RL 策略已经表现出来的能力。',
    roleInPipeline: '它把昂贵训练与推理计算压回可部署模型，并在量化、合并和服务交付中守住能力。',
    learningArc: ['先从多采样选优制造数据', '再比较多种蒸馏信号', '最后处理 on-policy 教学和部署一致性'],
  },
  industry: {
    plainDefinition: '工业后训练不是选一个最流行的算法，而是找到当前产品瓶颈，用最小可靠实验验证净收益。',
    roleInPipeline: '这一章把前面的方法放回真实约束：数据、算力、延迟、安全、维护、回滚和团队协作。',
    learningArc: ['先建立选型顺序', '再区分论文结论与生产证据', '最后形成可执行的 90 天项目路线'],
  },
}

export const lessonGuides: Record<string, LessonGuide> = {
  'sft-objective': guide(
    '模型原本只擅长“接着写”，SFT 用成套对话示范告诉它：看到用户问题时，哪些 token 才是应该生成的助手回答。',
    '像给新员工看标准客服记录。员工不是背每句话，而是在学习遇到某类问题时应该用什么方式回应。',
    '如果不先理解输入、标签和 mask，训练 loss 即使下降，也可能是在让模型复述用户问题或学习错误角色。',
    [['整理对话', '把 system、user、assistant 消息按同一 chat template 转成 token。'], ['制作标签', '输入 token 全部作为条件，但通常只让 assistant token 参与 loss。'], ['前向与反向', '模型预测下一个 token，交叉熵把错误传回可训练参数。'], ['用生成验证', '训练后用未见过的指令检查行为，而不是只看训练 loss。']],
    code('用 TRL 跑一个最小 SFT', 'python', [
      'from trl import SFTConfig, SFTTrainer',
      '',
      'config = SFTConfig(',
      '    output_dir=\"runs/sft\",',
      '    assistant_only_loss=True,',
      '    max_length=2048,',
      ')',
      'trainer = SFTTrainer(',
      '    model=\"Qwen/Qwen3-0.6B\",',
      '    args=config,',
      '    train_dataset=train_dataset,',
      ')',
      'trainer.train()',
    ], ['train_dataset 应保存结构化 messages，而不是提前手拼不透明字符串。', 'assistant_only_loss 依赖 chat template 能返回 assistant mask；训练前必须抽样 decode 检查。', 'max_length 是截断上限，不等于所有样本都要补到该长度。']),
    'token-mask',
  ),
  'sft-data': guide(
    'SFT 数据工程就是决定“模型要模仿哪些老师、做哪些题、犯错样本如何被挡在训练集外”。',
    '像编一本教材：题量大不代表教材好，章节比例、答案正确性和是否覆盖真实考试更重要。',
    '模型会忠实复制数据里的答案、格式和偏差；数据定义不清时，增加训练步数只会更快学错。',
    [['画能力地图', '先按真实流量拆出问答、抽取、代码、工具、安全等能力桶。'], ['统一数据契约', '记录来源、许可、难度、生成器、验证器与失败原因。'], ['过滤与去重', '先规则和执行验证，再语义去重，最后做人工分层抽检。'], ['按短板混合', '按有效 token 和业务价值采样，并固定独立验证集。']],
    code('构造可审计样本', 'python', [
      'def build_example(raw):',
      '    answer = normalize(raw[\"answer\"])',
      '    checks = run_verifiers(raw[\"prompt\"], answer)',
      '    return {',
      '        \"messages\": [',
      '            {\"role\": \"user\", \"content\": raw[\"prompt\"]},',
      '            {\"role\": \"assistant\", \"content\": answer},',
      '        ],',
      '        \"source\": raw[\"source\"],',
      '        \"domain\": classify_domain(raw),',
      '        \"difficulty\": estimate_difficulty(raw),',
      '        \"checks\": checks,',
      '    }',
      '',
      'dataset = dataset.map(build_example).filter(lambda x: all(x[\"checks\"].values()))',
    ], ['不要在 filter 后丢掉失败原因，失败集是改进生成器和验证器的重要资产。', '训练、验证、金丝雀集应按来源分组切分，避免同源近重复泄漏。', '最终抽样比例要按 token 复核，长答案会悄悄改变样本数比例。']),
  ),
  'sft-systems': guide(
    '这一课解决“哪些参数要更新、显存花在哪里、训练是不是只在记数据”三个问题。',
    '像装修房子：全参微调相当于重装整屋，LoRA 是加可拆卸模块；选择取决于改动范围，不是越省越好。',
    '参数更新方式会影响容量、成本和合并交付，但它不能修复坏数据，也不能用更大的 rank 保证更好效果。',
    [['估算资源', '分开计算权重、梯度、优化器状态、激活与通信 buffer。'], ['选择更新范围', '小数据或快速迭代先 LoRA，大规模能力迁移再比较全参。'], ['跑小规模探针', '固定数据和评测，对 rank、target modules、学习率做消融。'], ['监控遗忘', '同时查看新任务、通用能力、格式和安全回归。']],
    code('LoRA 配置与训练', 'python', [
      'from peft import LoraConfig',
      'from trl import SFTConfig, SFTTrainer',
      '',
      'peft = LoraConfig(',
      '    r=16,',
      '    lora_alpha=32,',
      '    target_modules=[\"q_proj\", \"k_proj\", \"v_proj\", \"o_proj\"],',
      ')',
      'args = SFTConfig(output_dir=\"runs/lora\", learning_rate=2e-4)',
      'trainer = SFTTrainer(model=model, args=args, peft_config=peft, train_dataset=data)',
      'trainer.train()',
    ], ['target_modules 要与具体模型层名匹配，不能从其他架构直接复制。', 'LoRA 通常可承受比全参更高的学习率，但仍需用验证集确认。', '记录有效 tokens/s、峰值显存与最终任务指标，才能比较方案。']),
    'memory-budget',
  ),
  'sft-multiturn-tools': guide(
    '多轮与工具 SFT 教模型的不只是回答文本，还包括“何时调用工具、参数怎么填、拿到结果后怎么继续”。',
    '像训练值班工程师：除了会解释问题，还要会开工单、查监控，并根据查询结果决定下一步。',
    '任何一处 role、tool_call_id 或工具 schema 对不上，模型就会学到无法在真实 Harness 中执行的轨迹。',
    [['定义动作空间', '先冻结工具名、参数 schema、错误语义与输出格式。'], ['保存完整轨迹', '保留 assistant tool call、tool observation 和后续回答。'], ['设置 loss 边界', '明确工具结果是否参与预测，通常工具输出只作为条件。'], ['做扰动评测', '替换观察值或注入工具错误，检查模型是否真的读取结果。']],
    code('工具轨迹的数据形状', 'json', [
      '{',
      '  \"messages\": [',
      '    {\"role\": \"user\", \"content\": \"查询订单 A102\"},',
      '    {\"role\": \"assistant\", \"tool_calls\": [{',
      '      \"id\": \"call_1\", \"name\": \"get_order\",',
      '      \"arguments\": {\"order_id\": \"A102\"}',
      '    }]},',
      '    {\"role\": \"tool\", \"tool_call_id\": \"call_1\", \"content\": \"{\\\"status\\\": \\\"shipped\\\"}\"},',
      '    {\"role\": \"assistant\", \"content\": \"订单已发货。\"}',
      '  ]',
      '}',
    ], ['tool_call_id 必须把请求和观察一一对应。', '训练样本里的 tools schema 应与线上实际 schema 同版本。', '多模态样本还要验证图像 token 没有被 max_length 截断。']),
  ),
  'sft-long-context': guide(
    '长上下文训练不是把 max_length 调大，而是让更多有效 token 在显存预算里被正确注意、正确计损失。',
    '像往货车里装箱：packing 能减少空隙，但不同客户的箱子不能互相打开；长度上限也不能把关键标签切掉。',
    '错误 packing、截断和位置分布会让吞吐看起来提高，却损害跨段污染、答案缺失和长程检索能力。',
    [['先看长度分布', '统计 P50/P90/P99 和角色位置，不用平均值代表全部。'], ['制定截断规则', '优先保留任务所需证据与完整 answer，记录被截断比例。'], ['选择 packing', '保证样本边界的 attention、position 与 loss mask 语义正确。'], ['测有效吞吐', '用参与 loss 的有效 token，而非 padding 后 token 计算效率。']],
    code('计算有效 Token 利用率', 'python', [
      'def batch_stats(input_ids, attention_mask, labels):',
      '    allocated = input_ids.numel()',
      '    attended = attention_mask.sum().item()',
      '    supervised = (labels != -100).sum().item()',
      '    return {',
      '        \"attention_util\": attended / allocated,',
      '        \"loss_util\": supervised / allocated,',
      '        \"supervised_tokens\": supervised,',
      '    }',
    ], ['attention_util 衡量 padding 浪费，loss_util 还考虑 assistant-only mask。', '两者低的原因不同：前者用 packing 改善，后者可能是对话本身提示较长。', '统计时同时记录截断率，不能用高利用率掩盖信息丢失。']),
  ),
  'sft-repro': guide(
    '可复现训练就是让你几周后仍能回答：这批权重由哪份数据、哪套代码、哪些随机性和哪个基座产生。',
    '像实验室样品要贴完整标签；只有一个 checkpoint 文件名，无法知道配方和生产过程。',
    '后训练差异经常小于数据漂移和推理配置差异，缺少版本记录会让实验结论无法归因。',
    [['冻结输入版本', '记录数据 manifest、tokenizer、template、base model 与代码提交。'], ['固定随机源', '设置数据打乱、初始化、采样与分布式 seed。'], ['保存可恢复状态', 'checkpoint 同时包含优化器、scheduler、步数和 RNG 状态。'], ['建立复现实验', '从中途恢复并验证后续 loss、样本顺序和评测在容差内一致。']],
    code('最小实验清单', 'yaml', [
      'run_id: sft-domain-v3',
      'git_commit: 8bd3a78',
      'base_model: Qwen/Qwen3-0.6B',
      'dataset_manifest: data/manifest-v3.json',
      'chat_template_sha256: \"...\"',
      'seed: 42',
      'precision: bf16',
      'gradient_accumulation_steps: 16',
      'resume_from: checkpoints/step-1200',
      'eval_suite: evals/v5.yaml',
    ], ['manifest 应保存每个分片哈希和过滤器版本。', '恢复训练时检查 global batch 与 world size 是否改变。', '评测也要固定 tokenizer、sampling、stop token 和 judge 版本。']),
  ),

  'preference-data': guide(
    '偏好数据的最小单位是：同一个问题、两个候选回答，以及“哪个更好”的判断。',
    '像品尝两杯咖啡时做二选一，通常比给每杯打 87 分更稳定；但杯子顺序和评分标准仍会影响判断。',
    '如果候选差距太大、位置固定或 chosen 总是更长，模型会学会这些捷径，而不是学习真正的质量。',
    [['先写 rubric', '分开判断正确、帮助、安全和风格，允许无法判断。'], ['生成有信息量的 pair', '让候选质量接近决策边界，而不是大量碾压局。'], ['随机展示顺序', '保存原始身份和展示位置，测量位置偏差。'], ['审计一致性', '计算标注员一致率，并把高风险分歧样本送仲裁。']],
    code('随机化 A/B 并还原标签', 'python', [
      'import random',
      '',
      'def make_task(pair):',
      '    swapped = random.random() < 0.5',
      '    left, right = (pair[\"rejected\"], pair[\"chosen\"]) if swapped else (pair[\"chosen\"], pair[\"rejected\"])',
      '    return {\"left\": left, \"right\": right, \"swapped\": swapped}',
      '',
      'def recover_choice(task, picked_side):',
      '    picked_chosen = (picked_side == \"left\") != task[\"swapped\"]',
      '    return \"chosen\" if picked_chosen else \"rejected\"',
    ], ['展示顺序随机化必须和原始 chosen/rejected 身份分开保存。', '同一 pair 交换顺序重标可直接估计位置敏感度。', '允许 tie，强制二选一会制造伪偏好。']),
    'preference-pair',
  ),
  'reward-model': guide(
    '奖励模型把“回答 A 比 B 好”压缩成一个可比较的分数差，后续策略会努力得到更高分。',
    '像训练一位裁判：它不是写答案，而是看到两份答案后判断谁更符合规则。',
    '裁判一旦有漏洞，RL 会主动找到并放大漏洞，所以验证奖励模型要比普通分类器更严格。',
    [['编码两个回答', '同一模型分别输出 chosen 和 rejected 的标量 reward。'], ['只看分数差', 'Bradley–Terry loss 让 chosen 分数相对更高。'], ['校准决策边界', '按领域、长度和难度看准确率与置信度。'], ['做反事实攻击', '只改长度、格式或加入可验证错误，检查裁判是否走捷径。']],
    code('Bradley–Terry 成对损失', 'python', [
      'import torch.nn.functional as F',
      '',
      'def pairwise_reward_loss(chosen_reward, rejected_reward):',
      '    margin = chosen_reward - rejected_reward',
      '    loss = -F.logsigmoid(margin).mean()',
      '    accuracy = (margin > 0).float().mean()',
      '    return loss, {\"pair_accuracy\": accuracy, \"margin\": margin.mean()}',
    ], ['loss 只约束差值，reward 的绝对零点没有可辨识意义。', 'accuracy 高仍可能来自长度捷径，因此必须分桶报告。', '策略优化关心训练分布外的 reward 行为，需加入对抗 pair。']),
  ),
  rlaif: guide(
    'RLAIF 用强模型按照人写的原则做比较、批评和修订，再把这些结果变成 SFT 或偏好数据。',
    '像让资深审核员先按制度批改大量作业，人类负责写制度和抽查，而不是逐份从头评分。',
    'AI Judge 可以扩量，但会带入位置、长度、自偏好和提示敏感性，必须用人工金标校准。',
    [['把原则写成 rubric', '原则要能落到可观察行为，避免“回答要好”这种空话。'], ['生成独立判断', '先交换顺序分别判断，再处理一致和分歧结果。'], ['用人工锚点校准', '按领域测 judge 与人工的一致性和置信度。'], ['分层使用反馈', '规则任务用 verifier，高风险人工，开放长尾用校准 judge。']],
    code('双顺序 Judge 校准', 'python', [
      'def robust_judge(prompt, answer_a, answer_b):',
      '    first = judge(prompt, left=answer_a, right=answer_b)',
      '    second = judge(prompt, left=answer_b, right=answer_a)',
      '    second = swap_label(second)',
      '    if first.label != second.label:',
      '        return {\"label\": \"needs_review\", \"confidence\": 0.0}',
      '    return {\"label\": first.label, \"confidence\": min(first.confidence, second.confidence)}',
    ], ['交换候选顺序后必须把第二次标签映射回原身份。', '分歧样本不应强行取多数，可进入人工仲裁或主动学习池。', '同时记录 judge 模型、prompt 和 rubric 版本。']),
  ),
  'preference-multiobjective': guide(
    '真实偏好往往是多科成绩：正确性、帮助性、安全、风格和成本可能彼此冲突。',
    '像选车不能只看一个总分；油耗、安全和空间的权重取决于使用场景，还可能有绝不能违反的底线。',
    '直接把不同尺度的 reward 相加会掩盖冲突，模型可能用风格分补偿事实错误或安全违规。',
    [['拆开目标', '每个 reward 分量单独定义数据、尺度和失败样例。'], ['先设硬约束', '安全、合规和格式底线不应只靠小权重惩罚。'], ['再做权衡', '在满足约束的策略中比较质量、成本和延迟。'], ['监控 Pareto 移动', '每次训练同时报告所有分量和分桶胜率。']],
    code('约束优先的奖励聚合', 'python', [
      'def constrained_reward(scores):',
      '    if scores[\"safety\"] < 0.95:',
      '        return -10.0, {\"blocked_by\": \"safety\"}',
      '    utility = (',
      '        0.55 * zscore(scores[\"correctness\"], \"correctness\")',
      '        + 0.30 * zscore(scores[\"helpfulness\"], \"helpfulness\")',
      '        - 0.15 * zscore(scores[\"cost\"], \"cost\")',
      '    )',
      '    return utility, {\"blocked_by\": None}',
    ], ['硬约束阈值要在独立安全集上校准。', '不同 reward 先标准化，否则数值范围会偷偷决定权重。', '训练日志必须保留各分量，不能只存聚合 reward。']),
  ),
  'reward-uncertainty': guide(
    '奖励不确定性表示“裁判自己也没把握”，它能帮助我们找到需要更多标注或应降低优化强度的样本。',
    '像多位裁判打分：平均分相同的两份答案，裁判意见高度分裂的那份风险更高。',
    '单一奖励模型的高分容易被当成事实；集成分歧和校准误差能暴露分布外样本。',
    [['训练多样裁判', '使用不同 seed、数据切分或架构训练 reward ensemble。'], ['计算均值与分歧', '均值代表偏好估计，方差近似认识不确定性。'], ['做置信度校准', '检查预测胜率与实际人工胜率是否匹配。'], ['用于采样和约束', '优先标注高分歧样本，或对高不确定 reward 降权。']],
    code('集成奖励的保守分数', 'python', [
      'import torch',
      '',
      'def ensemble_score(models, batch, penalty=1.0):',
      '    rewards = torch.stack([model(batch) for model in models])',
      '    mean = rewards.mean(dim=0)',
      '    std = rewards.std(dim=0)',
      '    conservative = mean - penalty * std',
      '    return conservative, {\"reward_mean\": mean, \"reward_std\": std}',
    ], ['模型必须有足够多样性，否则相同偏差会让方差虚假地低。', 'mean-std 是工程启发式，不代表严格置信下界。', '监控策略是否逐渐偏向 ensemble 分歧最大的区域。']),
  ),

  'rlhf-pipeline': guide(
    '经典 RLHF 是一个循环：Actor 生成回答，Reward 打分，Critic 估计每一步值不值，最后 PPO 更新 Actor。',
    '像运动员、裁判和教练一起训练：运动员尝试，裁判给结果分，教练判断哪一步贡献大，再调整动作。',
    '四个逻辑角色混在一起理解，会分不清 KL、value 和 reward 各自在控制什么。',
    [['Rollout', 'Actor 从 prompt 生成回答，同时保存旧策略 logprob。'], ['Scoring', 'Reward Model、规则和 reference KL 为轨迹生成奖励。'], ['Advantage', 'Critic 与 GAE 把序列奖励分配到 token。'], ['Update', 'PPO 用有限 epoch 更新 Actor 和 Critic，再同步新权重。']],
    code('最小在线 RLHF 循环', 'python', [
      'for prompts in prompt_loader:',
      '    trajectories = actor.generate(prompts, return_logprobs=True)',
      '    rm_score = reward_model.score(trajectories)',
      '    ref_logps = reference.logprobs(trajectories)',
      '    rewards = add_kl_penalty(rm_score, trajectories.logps, ref_logps)',
      '    values = critic.predict(trajectories)',
      '    advantages, returns = compute_gae(rewards, values)',
      '    ppo_update(actor, critic, trajectories, advantages, returns)',
      '    rollout_engine.sync_weights(actor)',
    ], ['reference 固定，用于约束策略偏离起点；它不是 reward model。', '旧 logprob 必须来自生成轨迹时的策略版本。', 'rollout 与 update 的权重版本要进入 trace，便于分析 policy lag。']),
    'rlhf-loop',
  ),
  'ppo-math': guide(
    'PPO 的核心是：好动作可以提高概率，坏动作可以降低概率，但每次别改得太猛。',
    '像根据一次考试调整学习计划，可以加大有效方法，但不能一次把所有时间都押在一道题型上。',
    'clip 只在当前批数据附近提供保守更新，不是万能稳定器；优势符号、旧概率和 mask 错一个都会反向训练。',
    [['算概率比', '比较当前策略和采样旧策略对同一 token 的概率。'], ['乘优势', '优势为正就鼓励，为负就抑制。'], ['执行裁剪', '把过大的概率变化限制在 1±epsilon 附近。'], ['联合更新', '叠加 value loss、entropy 与 reference KL，并监控各项。']],
    code('PPO clipped policy loss', 'python', [
      'import torch',
      '',
      'def ppo_policy_loss(new_logp, old_logp, advantage, mask, eps=0.2):',
      '    ratio = torch.exp(new_logp - old_logp)',
      '    unclipped = ratio * advantage',
      '    clipped = ratio.clamp(1 - eps, 1 + eps) * advantage',
      '    objective = torch.minimum(unclipped, clipped)',
      '    loss = -(objective * mask).sum() / mask.sum()',
      '    clip_fraction = ((ratio - 1).abs() > eps).float()[mask.bool()].mean()',
      '    return loss, clip_fraction',
    ], ['目标是最大化 surrogate，因此代码最小化它的负值。', 'mask 只覆盖 response 中有效 token，不能把 prompt padding 混进去。', 'clip_fraction 很高通常意味着学习率、epoch 或 advantage 尺度过大。']),
    'ppo-clip',
  ),
  'ppo-debug': guide(
    'PPO 调试不是只问“loss 为什么不降”，而是同时看 reward、KL、熵、长度、clip fraction 和人工质量是否朝同一方向变化。',
    '像看病不能只量体温；不同指标组合才能区分感染、脱水还是药物副作用。',
    'reward 上升可能只是漏洞被利用，KL 过低也可能是模型根本没学，必须用失败样本和时间序列一起诊断。',
    [['先验证管线', '用极小 batch 检查 reward、mask、logprob 和 advantage 符号。'], ['观察联合曲线', '把 reward 分量、KL、entropy、length、clip fraction 对齐到 step。'], ['抽样看行为', '固定 prompt 对比 reference、旧策略和当前策略输出。'], ['逐项消融', '临时关闭某个 reward、KL 或长度项，确认因果来源。']],
    code('训练门禁与自动停机', 'python', [
      'def should_stop(metrics):',
      '    reasons = []',
      '    if metrics[\"kl_to_ref\"] > 0.25:',
      '        reasons.append(\"KL drift\")',
      '    if metrics[\"clip_fraction\"] > 0.5:',
      '        reasons.append(\"updates too large\")',
      '    if metrics[\"reward\"] > metrics[\"reward_p95_train\"] and metrics[\"human_win_rate\"] < 0.5:',
      '        reasons.append(\"possible reward hacking\")',
      '    return reasons',
    ], ['阈值必须由任务和基线校准，示例数字不能直接用于生产。', '自动停机要保存 checkpoint、样本与指标快照。', 'human_win_rate 可用小规模人工或高可信 anchor 集近似。']),
  ),
  'policy-gradient-family': guide(
    '这些算法都在估计“某次采样比平常好多少”，差别主要在 baseline 从哪里来、是否训练 critic、样本怎么复用。',
    '像比较销售表现：可以和长期平均比、和同组同事比，或请经理预测合理业绩；不同基线影响噪声和成本。',
    '先理解 REINFORCE 的无偏梯度，再看 RLOO、ReMax 和 Actor–Critic 才不会把算法名当成互不相关的配方。',
    [['从回报出发', 'REINFORCE 用完整 return 乘 logprob 梯度。'], ['加入 baseline', '减去与动作无关的基线降低方差。'], ['选择基线来源', 'RLOO 用同组其他样本，ReMax 用贪心结果，Critic 学 value。'], ['比较系统代价', 'critic 增加显存与更新成本，多采样基线增加 rollout 成本。']],
    code('RLOO 的 leave-one-out 优势', 'python', [
      'import torch',
      '',
      'def rloo_advantages(rewards):',
      '    # rewards: [batch, samples_per_prompt]',
      '    group_sum = rewards.sum(dim=1, keepdim=True)',
      '    baseline = (group_sum - rewards) / (rewards.size(1) - 1)',
      '    return rewards - baseline',
      '',
      'advantages = rloo_advantages(group_rewards)',
      'loss = -(sequence_logps * advantages.detach()).mean()',
    ], ['每个候选的 baseline 不包含它自己，避免相关偏差。', 'samples_per_prompt 至少为 2，组太小会有高方差。', 'sequence_logps 的归一化方式会影响长度偏差。']),
  ),
  'kl-estimation': guide(
    'KL 用来衡量新策略离原模型有多远，熵用来衡量当前策略还保留多少选择空间。',
    '像导航时既要朝奖励高的方向走，也要用安全绳限制离熟悉区域太远，并避免只剩一条路线。',
    'KL 估计器、采样分布和聚合粒度不同会让数值不可直接比较，负的样本 KL 也不等于真实 KL 为负。',
    [['明确比较对象', '区分 current-old KL 与 current-reference KL。'], ['在采样 token 上估计', '保存 policy/ref logprob，并使用一致 mask。'], ['选择聚合方式', '按 token、sequence、prompt 分别报告，避免长度混淆。'], ['自适应约束', '根据目标 KL 调 β，但用质量门禁防止只追目标数字。']],
    code('按序列统计 sampled KL', 'python', [
      'def sampled_kl(policy_logp, ref_logp, response_mask):',
      '    token_log_ratio = policy_logp - ref_logp',
      '    token_mean = (token_log_ratio * response_mask).sum() / response_mask.sum()',
      '    seq_sum = (token_log_ratio * response_mask).sum(dim=-1)',
      '    seq_len = response_mask.sum(dim=-1)',
      '    return {\"token_kl\": token_mean, \"sequence_kl\": seq_sum.mean(), \"kl_per_token\": (seq_sum / seq_len).mean()}',
    ], ['这是当前策略样本上的 Monte Carlo 估计，单样本 log-ratio 可为负。', 'sequence_kl 会随长度变大，必须同时看 per-token 版本。', '不要把 PPO 新旧 ratio 的 clip 当作 reference KL 的替代。']),
  ),

  'dpo-core': guide(
    'DPO 让模型相对参考模型更加偏爱 chosen、更加远离 rejected，训练形式看起来像二分类。',
    '像比较两份作文：不仅看当前学生更喜欢哪份，还看这种偏好相对原始学生发生了多大变化。',
    'reference 项阻止模型仅凭原本就有的概率差获得奖励，β 控制偏好间隔与保守程度。',
    [['准备三段数据', '每条样本包含 prompt、chosen 和 rejected，模板边界必须一致。'], ['算四个 logprob', '策略与 reference 分别计算 chosen/rejected 的序列 logprob。'], ['形成相对间隔', '先做 policy-reference，再比较 chosen-rejected。'], ['优化 logsigmoid', '提高正间隔，同时监控 chosen 和 rejected 的绝对 logprob。']],
    code('TRL DPO 最小训练', 'python', [
      'from datasets import load_dataset',
      'from trl import DPOConfig, DPOTrainer',
      '',
      'data = load_dataset(\"trl-lib/ultrafeedback_binarized\", split=\"train\")',
      'args = DPOConfig(',
      '    output_dir=\"runs/dpo\",',
      '    beta=0.1,',
      '    max_length=2048,',
      ')',
      'trainer = DPOTrainer(model=\"Qwen/Qwen3-0.6B\", args=args, train_dataset=data)',
      'trainer.train()',
    ], ['显式 prompt + chosen + rejected 格式最容易审计。', 'ref_model 为空时，Trainer 通常以初始策略作为 reference；具体版本需查当前文档。', '除 loss 外还要看 rewards/margins、rewards/accuracies 与回答长度。']),
    'dpo-margin',
  ),
  'dpo-family': guide(
    'DPO 家族是在回答不同问题：偏好标签有噪声怎么办、只有好坏样本没有成对数据怎么办、长度偏差怎么处理。',
    '像同一门考试可以用选择题、及格线或排名制评分；规则不同，适合的数据也不同。',
    '算法名不是越新越好。先根据数据形态和失败模式选目标，再用同一基线和评测做小规模比较。',
    [['识别数据形态', 'pair、unpaired positive/negative、binary feedback 的统计含义不同。'], ['定位原始 DPO 失败', '检查长度、标签噪声、reference 质量和绝对 likelihood。'], ['选择最小变体', '只改变一项假设，固定数据和训练预算。'], ['做行为评测', '比较胜率、长度、KL、chosen/rejected logprob 与安全回归。']],
    code('按失败模式选择 loss', 'python', [
      'from trl import DPOConfig',
      '',
      'def make_config(problem):',
      '    if problem == \"noisy_pairs\":',
      '        return DPOConfig(loss_type=\"robust\", label_smoothing=0.1)',
      '    if problem == \"length_bias\":',
      '        return DPOConfig(loss_type=\"sigmoid_norm\")',
      '    if problem == \"identity_link\":',
      '        return DPOConfig(loss_type=\"ipo\")',
      '    return DPOConfig(loss_type=\"sigmoid\")',
    ], ['示例参数只是说明接口，不是推荐超参数。', '不同 TRL 版本支持的 loss_type 会变化，实验前锁定版本。', '若改变了数据预处理，就不能把差异完全归因于 loss。']),
  ),
  'offline-online': guide(
    '离线偏好优化只学习已经收集到的候选；在线方法会让当前模型重新生成，从新的失败和机会中继续学习。',
    '像只复习旧错题与参加新的模拟考试：旧题便宜稳定，新考试能暴露当前水平的新问题。',
    '当策略已经超过旧 chosen 时，继续离线 DPO 可能压低更好的新行为；但在线采样也增加成本和风险。',
    [['测支持集覆盖', '比较当前策略输出与历史 chosen 的质量和风格。'], ['建立离线强基线', '先用 SFT/DPO 验证反馈信号是否可靠。'], ['小规模在线采样', '只在高价值 prompt 上生成新 pair 或 reward。'], ['设升级门槛', '只有稳定增益覆盖系统和标注成本时才扩大在线 RL。']],
    code('检测偏好数据是否过时', 'python', [
      'def support_audit(policy, preference_rows, judge):',
      '    wins = 0',
      '    for row in preference_rows:',
      '        current = policy.generate(row[\"prompt\"])',
      '        wins += judge(row[\"prompt\"], current, row[\"chosen\"]) == \"current\"',
      '    return wins / len(preference_rows)',
      '',
      'if support_audit(policy, holdout_pairs, judge) > 0.7:',
      '    refresh_preference_data(policy)',
    ], ['阈值应结合人工抽检和分桶结果，不宜只依赖同一个 judge。', '审计集必须与训练 pair 隔离。', '刷新数据时保留旧分布作为回归锚点。']),
  ),
  'dpo-implementation': guide(
    'DPO 最常见的错误不在公式，而在 prompt 边界、padding、截断和序列 logprob 聚合。',
    '像财务公式完全正确，但把两个月份或两种币种的数字放到一起，结果仍然会错。',
    '手写 loss 前应先构造一个能人工算出的极小 batch，验证每个 token mask 和四个 logprob。',
    [['统一 tokenization', 'chosen/rejected 共享完全相同的 prompt token。'], ['只聚合 completion', 'padding 与 prompt token 不进入序列 logprob。'], ['冻结 reference', 'reference 不反向传播，必要时预计算 logprob。'], ['验证方向', '人工构造 chosen 概率更高的样本，loss 应更小。']],
    code('手写 DPO 核心损失', 'python', [
      'import torch.nn.functional as F',
      '',
      'def dpo_loss(pi_c, pi_r, ref_c, ref_r, beta=0.1):',
      '    policy_margin = pi_c - pi_r',
      '    reference_margin = ref_c - ref_r',
      '    logits = beta * (policy_margin - reference_margin)',
      '    loss = -F.logsigmoid(logits).mean()',
      '    return loss, {\"implicit_margin\": logits.mean()}',
    ], ['四个输入都是 completion token logprob 的序列聚合。', '若按长度平均，目标已经不再是原始 sequence-sum DPO，需要明确记录。', '同时监控 pi_c 和 pi_r，避免仅通过猛烈压低 rejected 获得 margin。']),
  ),

  rlvr: guide(
    'RLVR 用程序直接检查答案是否正确：代码跑测试、数学验结果、SQL 查执行输出，过关就得奖励。',
    '像练习题有自动判题器，学生可以反复尝试，不必每次等老师主观打分。',
    '验证器越硬，奖励越可靠；但验证器覆盖不全时，模型会寻找测试漏洞而不是真正解决任务。',
    [['定义可执行任务', '环境、输入、期望属性和资源限制都要版本化。'], ['生成多个候选', '让策略探索不同推理或程序路径。'], ['隔离执行验证', '在沙箱中运行隐藏测试，限制时间、网络和文件权限。'], ['回传结构化奖励', '区分编译、部分测试、全通过和违规，不只给一个黑盒分数。']],
    code('代码任务验证器骨架', 'python', [
      'def verify_submission(code, hidden_tests, sandbox):',
      '    result = sandbox.run(code, tests=hidden_tests, timeout_seconds=5)',
      '    if result.policy_violation:',
      '        return -1.0, {\"reason\": \"policy_violation\"}',
      '    if result.timed_out:',
      '        return 0.0, {\"reason\": \"timeout\"}',
      '    score = result.passed / result.total',
      '    return score, {\"passed\": result.passed, \"total\": result.total}',
    ], ['隐藏测试不能从工具输出或训练数据泄漏。', '沙箱本身是安全边界，不能让生成代码直接运行在宿主机。', '部分奖励可提高密度，但也可能改变最优策略，需做消融。']),
  ),
  grpo: guide(
    'GRPO 对同一道题生成一组答案，用组内平均水平当作临时基线：高于同组的鼓励，低于同组的抑制。',
    '像同一批选手做同一套题，按本组相对表现调整，而不额外请一位教练预测每个人应得多少分。',
    '省掉 Critic 能节省模型显存，但要付出多候选生成成本；全对或全错组没有相对信号。',
    [['按 prompt 组采样', '每个 prompt 用同一策略生成 G 个候选。'], ['逐个验证', '为每个 completion 计算 reward 和违规信息。'], ['组内标准化', '减均值并按标准差缩放，得到相对 advantage。'], ['做策略更新', '结合 ratio、clip 和 KL 更新，并过滤无信息组。']],
    code('TRL GRPO 自定义奖励', 'python', [
      'from trl import GRPOConfig, GRPOTrainer',
      '',
      'def exact_match_reward(completions, answer, **kwargs):',
      '    texts = [completion[0][\"content\"] for completion in completions]',
      '    return [1.0 if extract_final(x) == y else 0.0 for x, y in zip(texts, answer)]',
      '',
      'args = GRPOConfig(output_dir=\"runs/grpo\", num_generations=8)',
      'trainer = GRPOTrainer(',
      '    model=\"Qwen/Qwen3-0.6B\", args=args,',
      '    reward_funcs=exact_match_reward, train_dataset=dataset,',
      ')',
      'trainer.train()',
    ], ['reward 函数的参数形状依赖 TRL 版本和数据列，必须锁版本并写单元测试。', 'num_generations 会直接放大 rollout 成本。', '同时记录每组 reward 方差和全零/全一组比例。']),
    'grpo-group',
  ),
  'reasoning-behavior': guide(
    '长推理不是目标本身。真正目标是模型在难题上会探索、检查和纠错，并在简单题上及时停止。',
    '像解题草稿：需要时多写能减少错误，但把每道题都写成十页并不代表思考更好。',
    '只奖励最终正确容易让长度自然增长；如果评测不控 token 预算，就会把更多计算误判成更强能力。',
    [['建立冷启动', '先用少量可验证的高质量推理轨迹教会格式和基本策略。'], ['在线探索', '在适中难度题上多采样，让 reward 区分策略。'], ['拒绝采样回灌', '把验证通过且多样的轨迹作为新 SFT 数据。'], ['做预算控制评测', '同时比较固定 token、固定延迟和自然终止表现。']],
    code('保留正确且多样的推理轨迹', 'python', [
      'def select_reasoning_traces(candidates, verifier, embedder, max_keep=4):',
      '    valid = [x for x in candidates if verifier(x).passed]',
      '    valid.sort(key=lambda x: (x.token_count, -x.verifier_score))',
      '    selected = []',
      '    for item in valid:',
      '        if all(cosine(embedder(item), embedder(old)) < 0.92 for old in selected):',
      '            selected.append(item)',
      '        if len(selected) == max_keep:',
      '            break',
      '    return selected',
    ], ['先按正确性过滤，再控制长度和语义多样性。', '相似度阈值应在任务数据上校准。', '保留完整生成配置和 verifier 版本，避免不可复现。']),
  ),
  'modern-rl-algorithms': guide(
    'DAPO、Dr.GRPO、GSPO 和 REINFORCE++ 都在修补大规模推理 RL 的具体不稳定来源，而不是四套互不相干的魔法。',
    '像赛车调校：有人改采样，有人改长度归一化，有人把控制单位从每个零件改成整车。',
    '先定位是无信息 batch、长度偏差、ratio 粒度还是方差问题，再选择对应机制。',
    [['列出原始故障', '检查 reward 方差、长度相关性、clip fraction、KL 和极端 ratio。'], ['对应算法机制', '动态采样、token loss、去长度偏差、sequence ratio 各解决不同问题。'], ['固定公共基线', '相同模型、数据、rollout token 和 verifier 做公平比较。'], ['验证跨任务稳定性', '单一数学基准上的提升不能自动迁移到代码或 Agent。']],
    code('用配置显式记录算法差异', 'yaml', [
      'algorithm: grpo_ablation',
      'advantage:',
      '  normalization: group_std',
      'loss:',
      '  aggregation: token_mean',
      'ratio:',
      '  granularity: token',
      'sampling:',
      '  filter_zero_variance_groups: true',
      'length:',
      '  overlong_penalty: soft',
      'kl:',
      '  coefficient: 0.01',
    ], ['不要只记录一个算法名；具体实现选择才决定实验含义。', '每次 ablation 只改一个关键字段。', '配置与训练日志一起进入 artifact，方便复现和面试复盘。']),
  ),
  'process-outcome-credit': guide(
    'Outcome reward 只看最终答案，Process reward 会在中间步骤或每轮动作上给反馈。',
    '像项目只看最终交付与每周检查里程碑：前者简单客观，后者能早发现问题但可能诱导只做表面进度。',
    '过程奖励能降低信用分配难度，也会改变模型最优行为；错误 PRM 可能惩罚新颖但正确的路线。',
    [['确定奖励粒度', '先区分 token、step、turn 和 trajectory，各自对应什么可观察事件。'], ['标注或训练 PRM', '用局部正确性、可达性和工具状态定义监督。'], ['与终局结果组合', '过程分只做 shaping 时，确保不压过最终任务成功。'], ['做反事实验证', '替换中间步骤或交换顺序，检查 PRM 是否识别因果作用。']],
    code('从 turn reward 计算 return', 'python', [
      'def discounted_returns(turn_rewards, gamma=1.0):',
      '    returns = [0.0] * len(turn_rewards)',
      '    running = 0.0',
      '    for t in reversed(range(len(turn_rewards))):',
      '        running = turn_rewards[t] + gamma * running',
      '        returns[t] = running',
      '    return returns',
      '',
      'turn_advantages = normalize(discounted_returns(turn_rewards))',
    ], ['gamma=1 适合无时间偏好的有限轨迹，但长任务可能需要成本惩罚。', 'turn reward 与 token reward 分配要分开记录。', 'shaping 后必须验证最终 success 没有被局部分数替代。']),
  ),
  'test-time-training-time': guide(
    '训练时计算让模型参数永久改变，测试时计算让同一个模型在回答当前问题时多采样、搜索或调用工具。',
    '像平时训练提高基本功，与考试现场多检查几遍；二者都能提分，但成本发生在不同时间。',
    '比较模型能力时若不固定测试时预算，就无法判断增益来自参数还是来自更多采样。',
    [['先建单次基线', '固定温度、最大 token 和工具预算测 pass@1。'], ['增加测试时预算', '尝试 Best-of-N、rerank、tree search，画质量—成本曲线。'], ['把结果蒸馏回去', '将选优轨迹用于 SFT/DPO，观察 pass@1 是否提升。'], ['决定成本位置', '按线上请求量、延迟约束和训练频率比较长期总成本。']],
    code('比较训练时与测试时预算', 'python', [
      'def evaluate_budget(model, tasks, samples_per_task):',
      '    total_tokens, passed = 0, 0',
      '    for task in tasks:',
      '        candidates = [model.generate(task.prompt) for _ in range(samples_per_task)]',
      '        total_tokens += sum(x.token_count for x in candidates)',
      '        passed += any(task.verify(x.text) for x in candidates)',
      '    return {\"pass_rate\": passed / len(tasks), \"tokens_per_task\": total_tokens / len(tasks)}',
    ], ['同时报告 pass@1 和 pass@N，不能只展示最好候选。', '延迟还受并行度影响，token 总数与用户等待时间不是同一指标。', '蒸馏实验应重新用 N=1 评估是否形成持久能力。']),
  ),

  'agent-vs-workflow': guide(
    '普通 Chat 只生成一次回答；Workflow 按代码写好的步骤执行；Agent 则由模型根据观察决定下一步动作。',
    '像出行：Chat 给你路线建议，Workflow 是固定公交线路，Agent 像司机根据路况临时改道。',
    '不是所有任务都需要 Agent。步骤确定时用代码工作流更便宜、更可测；只有决策路径难以预先枚举时才值得开放循环。',
    [['先找不确定性', '确认任务是否真的需要模型在运行时选择下一步。'], ['定义状态与动作', '明确模型能看到什么、能调用什么、何时结束。'], ['建立工作流基线', '先用确定性代码完成可预知部分。'], ['只开放必要决策', '把不可枚举的节点交给 Agent，并设置预算与终止条件。']],
    code('最小 Agent Loop', 'python', [
      'def run_agent(task, model, tools, max_turns=8):',
      '    state = {\"task\": task, \"events\": []}',
      '    for turn in range(max_turns):',
      '        decision = model.decide(build_context(state, tools))',
      '        if decision.kind == \"final\":',
      '            return verify_final(decision.content, state)',
      '        call = validate_tool_call(decision, tools)',
      '        observation = tools.execute(call)',
      '        state[\"events\"].append({\"decision\": decision, \"observation\": observation})',
      '    raise RuntimeError(\"turn budget exceeded\")',
    ], ['循环必须有 max_turns、成本或时间预算，不能只依赖模型自觉停止。', '工具执行前由代码验证，模型输出不能直接变成副作用。', '每轮 decision 与 observation 都进入持久 trace。']),
    'agent-loop',
  ),
  'agent-model-protocol': guide(
    '一次模型调用就是应用发送一份上下文和工具清单，模型返回文本或结构化工具请求；所谓“多轮记忆”，本质是 Harness 在下一次调用时把需要的状态重新关联起来。',
    '像给远程顾问发工单：每次通话都要提供本轮材料。顾问可以建议“查订单”，但真正查询、拿到结果并回传的是你的系统。',
    '协议层一旦把 role、call id、流式参数或历史顺序拼错，后面再高级的规划和 RL 都只是在学习一个线上无法正确执行的世界。',
    [['构造请求', '加入当前 instructions、input/history、可见 tools 和输出预算。'], ['读取模型项', '区分普通文本、结构化输出、tool call 与 incomplete/error。'], ['执行并配对', '完整接收 arguments 后校验执行，用同一 call id 回填结果。'], ['衔接下一轮', '保存或重建必要状态，并记录实际发送的 context manifest。']],
    code('OpenAI Responses 风格的工具回合示意', 'typescript', [
      'const first = await client.responses.create({',
      '  model, input: task, tools: visibleTools,',
      '});',
      'const call = first.output.find(item => item.type === "function_call");',
      'if (!call) return first.output_text;',
      '',
      'const args = schema.parse(JSON.parse(call.arguments));',
      'const result = await executeAuthorized(call.name, args);',
      'return client.responses.create({',
      '  model, previous_response_id: first.id,',
      '  input: [{ type: "function_call_output", call_id: call.call_id, output: JSON.stringify(result) }],',
      '});',
    ], ['示例展示协议形状，不代表省略 policy、sandbox、timeout 和 trace。', '不要假设上一轮 instructions 会被所有 API 自动继承。', 'Streaming 时必须等 function call 参数块完成后再 JSON.parse。']),
    'model-protocol',
  ),
  'agent-loop-runtime': guide(
    '最小 Agent Loop 每轮只做一件事：准备工作材料，让模型提出下一步，通过多道闸门执行，再把真实观察写回；没有工具调用只表示“可以考虑停止”。',
    '像机场安检：旅客提出要带什么上机，系统依次核对物品、规则和许可，真正放行后才产生结果；任何拒绝也要给出清楚回执。',
    'Loop 的代码可以很短，但真实故障集中在工具定位、参数校验、审批、调用/结果配对、并发冲突和停止条件。',
    [['组装并调用', '从任务、状态、历史与工具投影构建本轮请求。'], ['识别决策', '读取 final、tool call、incomplete 或 error，而非假设永远有文本。'], ['通过五道闸门', 'Locate、Validate、Hook、Authorize 后才 Execute。'], ['回填或终止', '持久化成对结果，检查 verifier、预算、人工状态和进展。']],
    code('带执行闸门的最小循环', 'python', [
      'def run(task, max_turns=12):',
      '    state = store.create(task)',
      '    for _ in range(max_turns):',
      '        output = model.call(assemble(state, registry.project(state)))',
      '        if output.is_final:',
      '            return finish_if_verified(output, state)',
      '        for proposal in output.tool_calls:',
      '            tool = registry.locate(proposal.name)',
      '            call = tool.validate(proposal.arguments)',
      '            call = hooks.before_tool(call, state)',
      '            grant = policy.authorize(call, state)',
      '            result = executor.run(call, grant)',
      '            store.append_pair(state, proposal, result)',
      '        ensure_forward_progress(state)',
      '    return stop("turn_budget_exhausted", state)',
    ], ['每个失败也要形成 result，避免悬空 tool call。', 'finish_if_verified 可拒绝过早结束并把验证失败送回模型。', '并行执行只适用于无冲突调用，结果仍按 call id 稳定关联。']),
    'intent-gate',
  ),
  'agent-tools': guide(
    '工具是 Agent 可以请求执行的受控函数；一个好工具要让模型容易选对，也让系统能验证、授权和恢复。',
    '像给新员工一个操作台：按钮名字、必填项和错误提示越清楚，误操作越少。',
    '很多“模型不会调用工具”其实是 schema 含糊、工具重叠或错误信息不可行动，而不是模型能力不足。',
    [['缩小工具职责', '每个工具完成一个明确动作，名称和描述体现何时使用。'], ['收紧参数 schema', '使用 enum、范围和业务标识符，拒绝自由文本万能参数。'], ['设计错误语义', '区分可重试、需改参数、权限不足和永久失败。'], ['保护写操作', '加入幂等键、dry-run、审批和影响范围。']],
    code('Typed 工具与幂等写入', 'python', [
      'from pydantic import BaseModel, Field',
      '',
      'class RefundInput(BaseModel):',
      '    order_id: str = Field(pattern=r\"^A[0-9]{3,}$\")',
      '    amount_cents: int = Field(gt=0, le=100_000)',
      '    idempotency_key: str',
      '',
      'def refund_order(raw):',
      '    request = RefundInput.model_validate(raw)',
      '    cached = result_store.get(request.idempotency_key)',
      '    if cached:',
      '        return cached',
      '    return payment_api.refund(**request.model_dump())',
    ], ['Pydantic 验证只解决结构，不替代用户权限和业务规则。', '幂等键与结果必须持久化，否则进程重启后仍会重复执行。', '错误返回应包含稳定 code 和可行动信息，避免模型盲目重试。']),
  ),
  'agent-planning-memory': guide(
    '规划是决定接下来做什么，状态是当前任务真实进展，记忆是从过去提取出来、可能对现在有用的信息。',
    '像做项目：计划表可以改，项目数据库记录事实，经验手册保存长期规律；三者不能混成一段聊天历史。',
    '把所有历史都塞进 prompt 会造成成本增长、过期信息和错误记忆，可靠 Agent 需要结构化状态和选择性检索。',
    [['分离事实与想法', '工具结果和已完成动作进入结构化 state，模型计划只是可修改假设。'], ['定义短期工作集', '只把当前步骤所需证据、约束和最近事件放入上下文。'], ['写入长期记忆', '仅保存经验证、可归属、可过期的信息。'], ['恢复与复盘', 'checkpoint 保存状态版本，resume 时重新验证外部世界是否变化。']],
    code('状态与记忆分层', 'python', [
      'from dataclasses import dataclass, field',
      '',
      '@dataclass',
      'class RunState:',
      '    goal: str',
      '    status: str = \"ready\"',
      '    completed_steps: list[str] = field(default_factory=list)',
      '    facts: dict = field(default_factory=dict)',
      '    pending_action: dict | None = None',
      '',
      'def assemble_context(state, memory_store, token_budget):',
      '    memories = memory_store.search(state.goal, filters={\"verified\": True})',
      '    return pack_minimum_working_set(state, memories, token_budget)',
    ], ['state 是任务事实来源，不应从模型自然语言总结反推。', '长期记忆写入需要来源、时间和权限标签。', '恢复 pending_action 前先查询外部系统，避免重复副作用。']),
  ),
  'multi-agent': guide(
    '多 Agent 是把不同职责交给多个上下文或模型实例，但它不会自动让结果更聪明。',
    '像组建团队：分工清楚能并行提速，职责重叠则会增加会议、传话和冲突。',
    '先决定谁拥有最终答案和共享状态，再选择 manager、handoff 或并行；否则只会把单 Agent 错误变成分布式错误。',
    [['划分边界', '按能力、数据权限或独立子任务拆分，不按炫酷角色名拆分。'], ['确定控制权', 'Manager 保持最终责任；Handoff 让专家接管当前会话。'], ['并行独立工作', '只有无前置依赖的子任务才并行，并统一结果 schema。'], ['合并与仲裁', '由代码或负责人处理冲突、超时和部分失败。']],
    code('Manager 并行调用专家', 'python', [
      'import asyncio',
      '',
      'async def research_and_risk(task):',
      '    facts_task = asyncio.create_task(research_agent.run(task))',
      '    risk_task = asyncio.create_task(risk_agent.run(task))',
      '    facts, risks = await asyncio.gather(facts_task, risk_task)',
      '    return await manager_agent.run({',
      '        \"task\": task, \"facts\": facts.output, \"risks\": risks.output',
      '    })',
    ], ['并行前确认两个专家不会写同一资源。', '每个结果要有结构化 schema、来源和置信边界。', 'Manager 仍需验证事实，不能把“多个 Agent 同意”当正确证明。']),
  ),

  'harness-anatomy': guide(
    'Harness 是 Agent 的操作系统：模型只负责提出下一步，Harness 负责上下文、状态、工具、权限、执行、恢复和观测。',
    '像飞机驾驶舱：飞行员做决策，但仪表、限位、检查单和黑匣子共同保证系统可控。',
    '把所有责任推给 system prompt 会得到不可审计的软约束；可靠边界必须由运行时代码执行。',
    [['接收任务', '创建 run、绑定用户权限、预算和版本。'], ['组装上下文', '选择指令、状态、记忆、工具和当前观察。'], ['执行决策', '解析模型输出，做策略检查后调用工具。'], ['持久化与观测', '原子记录状态转换、结果、成本和错误，再决定继续或停止。']],
    code('可暂停的运行状态机', 'typescript', [
      'type RunStatus =',
      '  | \"ready\" | \"model_running\" | \"tool_pending\"',
      '  | \"waiting_approval\" | \"tool_running\"',
      '  | \"completed\" | \"failed\" | \"cancelled\";',
      '',
      'async function transition(runId: string, expected: RunStatus, next: RunStatus) {',
      '  return db.runs.compareAndSet(runId, { status: expected }, { status: next });',
      '}',
    ], ['compare-and-set 防止两个 worker 同时推进同一个 run。', '状态转换与 pending action 应在同一事务中持久化。', '暂停、取消和失败不是文本消息，而是明确的生命周期状态。']),
    'harness-map',
  ),
  'harness-task-contract': guide(
    'Task Spec 是一次 Agent 运行的合同：说清目标、约束、允许产物、怎样算成功，以及最多能花多少时间、token、工具调用和费用。',
    '像装修合同：只写“把房子装好”一定会争议；材料范围、验收标准、预算和变更流程都要提前落到纸面。',
    '模型停止说话不等于任务成功。只有成功标准与 verifier 使用同一来源，Harness 才能阻止过早结束并解释预算耗尽。',
    [['整理目标与约束', '把用户意图、项目规则和禁止事项合成可版本化目标。'], ['写 Done-when', '为最终状态、artifact、测试和安全结果定义机器可检查条件。'], ['分配多维预算', '同时限制 turn、token、工具、wall time、费用与审批。'], ['下发子任务合同', '对子 Agent 指定最小上下文、工具、权限、输出 schema 和截止条件。']],
    code('可执行的任务契约', 'typescript', [
      'type TaskSpec = {',
      '  goal: string;',
      '  constraints: string[];',
      '  doneWhen: { verifier: string; requiredArtifacts: string[] };',
      '  budget: { turns: number; tokens: number; toolCalls: number; wallMs: number };',
      '  allowedCapabilities: string[];',
      '};',
      '',
      'function canFinish(spec: TaskSpec, run: RunState) {',
      '  return verifierRegistry.run(spec.doneWhen.verifier, run) &&',
      '    spec.doneWhen.requiredArtifacts.every(id => run.artifacts.has(id));',
      '}',
    ], ['doneWhen 指向独立 verifier，而不是让模型输出 self_reported_success。', '预算耗尽与成功必须是不同终止原因。', 'Task Spec 与 evaluator 配置应共同版本化。']),
  ),
  'harness-tool-registry': guide(
    'Tool Registry 保存系统拥有的能力，但模型每一轮只应看到与当前任务、用户权限和阶段相符的最小工具投影。',
    '像公司应用商店：后台可以有上千项能力，但员工只看到岗位允许且当前任务用得上的那几项。',
    '全量工具 schema 会占 token、破坏稳定前缀、增加误选和攻击面；工具管理本身就是 context engineering。',
    [['注册真实能力', '保存 handler、schema、版本、风险、输出策略和依赖。'], ['按策略筛选', '依据用户、租户、任务阶段和 sandbox 生成候选集。'], ['渐进披露', '先给目录或 tool search，再延迟加载少量完整 schema。'], ['验证输出契约', '限制大小，标注来源，并为大结果提供持久 URI 与读取工具。']],
    code('运行时 Registry 与模型投影', 'typescript', [
      'const registry = new Map<string, RegisteredTool>();',
      '',
      'function projectTools(run: RunState) {',
      '  return [...registry.values()]',
      '    .filter(tool => policy.mayExpose(run.user, tool.capability))',
      '    .filter(tool => tool.stages.includes(run.stage))',
      '    .sort((a, b) => a.cacheOrder - b.cacheOrder)',
      '    .map(tool => ({ name: tool.name, description: tool.description, parameters: tool.schema }));',
      '}',
    ], ['模型可见投影不包含 handler 或秘密凭据。', 'Schema 版本要写入 trace，线上升级需兼容性回归。', 'Tool search 只解决发现，执行仍必须重新 authorize。']),
  ),
  'harness-context': guide(
    'Context Engineering 是在 token 预算和信任边界内，给模型准备“这一步最少但足够的工作材料”。',
    '像给现场维修员装工具包：不是把整个仓库搬过去，而是带上当前故障需要的图纸、零件和安全规程。',
    '上下文越长不一定越好；无关、过期或不可信内容会挤走关键约束，并扩大 prompt injection 面。',
    [['定义不可丢内容', '系统约束、当前目标、权威状态和审批结论优先。'], ['检索候选材料', '按任务、权限、时间和来源过滤记忆与文档。'], ['压缩与排序', '结构化 state 优于反复摘要，关键证据靠近当前决策。'], ['记录 provenance', '保存每段上下文的来源、版本和是否不可信。']],
    code('预算感知的 Context Assembler', 'python', [
      'def assemble_context(run, candidates, max_tokens):',
      '    required = [run.policy, run.goal, run.structured_state]',
      '    remaining = max_tokens - count_tokens(required)',
      '    selected = []',
      '    for item in rank_by_relevance_and_trust(candidates):',
      '        if item.tokens <= remaining and can_access(run.user, item):',
      '            selected.append(item)',
      '            remaining -= item.tokens',
      '    return render(required, selected, provenance=True)',
    ], ['required 内容也要有最大预算，超长 policy 应拆成可加载规则。', '权限过滤必须发生在内容进入模型之前。', '外部工具输出要标记为数据，不能当作更高优先级指令。']),
  ),
  'harness-context-recycling': guide(
    '上下文回收不是直接删除旧消息，而是先把完整事实放到可寻址的外部状态，再按信息损失从低到高减少模型此刻看到的内容。',
    '像整理书桌：先把整份档案归档并留下索引，再丢重复草稿，最后才把长期过程整理成交接摘要。',
    '长工具输出、反复摘要和不稳定前缀会同时影响正确性、成本与缓存命中；必须把截断、持久化、取回和缓存分开设计。',
    [['外置大结果', '完整输出存 artifact，context 保留摘要、hash、URI 和范围读取方法。'], ['裁剪可重建历史', '去除重复日志和已被结构化 state 覆盖的内容。'], ['局部压缩旧观察', '对早期 tool result 做 micro-compaction，并保留恢复入口。'], ['全局交接压缩', '只在高压时生成包含目标、约束、状态、pending action 和引用的可执行摘要。']],
    code('可恢复的长输出策略', 'python', [
      'def package_tool_output(call_id, raw, inline_limit=8_000):',
      '    if len(raw) <= inline_limit:',
      '        return {"call_id": call_id, "content": raw, "truncated": False}',
      '    artifact = artifacts.put(raw, content_type="text/plain")',
      '    return {',
      '        "call_id": call_id,',
      '        "content": summarize_head_tail(raw),',
      '        "truncated": True,',
      '        "artifact_uri": artifact.uri,',
      '        "sha256": artifact.sha256,',
      '    }',
    ], ['Truncated 必须显式标记，不能让模型误以为看到了完整结果。', 'Artifact 需要 range/search/read 能力，否则 URI 只是装饰。', '缓存诊断应记录实际前缀、命中 token 和压缩事件。']),
    'context-recycling',
  ),
  'harness-mcp': guide(
    'Tool 是能产生效果的执行能力，MCP 是连接外部能力的协议，Skill 是教 Agent 完成一类任务的方法；三者可以组合，但不能互相替代。',
    '像维修工作：扳手是 Tool，统一工具接口和借用流程像 MCP，维修手册是 Skill；车间管理系统 Harness 决定谁能借、在哪用和如何验收。',
    '把协议连接误当授权、把 Skill 指令误当执行器，都会模糊最关键的意图/效果边界。',
    [['按需加载 Skill', '先读稳定 manifest，匹配任务后再加载完整流程与依赖。'], ['通过 MCP 发现能力', 'Host 为 Server 建立 Client，读取 prompts/resources/tools。'], ['构建本地工具投影', '合并本地与远端工具，再按用户、任务和风险筛选。'], ['统一执行与审计', '无论工具来源，均经过 validate、authorize、sandbox、trace 和结果回填。']],
    code('MCP 工具调用消息', 'json', [
      '{',
      '  \"jsonrpc\": \"2.0\",',
      '  \"id\": 7,',
      '  \"method\": \"tools/call\",',
      '  \"params\": {',
      '    \"name\": \"get_order\",',
      '    \"arguments\": {\"order_id\": \"A102\"}',
      '  }',
      '}',
    ], ['调用前应先通过 tools/list 获得当前 schema，不能假设远端工具永不变化。', 'Skill 与 MCP Server 返回内容都应视为带来源的外部输入。', 'Host 需要把用户身份、租户和授权边界映射到每次调用。']),
    'tool-mcp-skill',
  ),
  'harness-safety': guide(
    'Policy 决定一次工具请求是允许、询问还是拒绝；Sandbox 则限制执行后真正能访问的文件、网络、进程和资源。',
    '像实验室：门禁审批决定谁能进入，防护柜和隔离区决定即使操作失误最多影响哪里。两层解决不同问题。',
    '只写 Prompt guardrail 既不是可靠授权，也不能阻止子进程、网络或文件系统副作用；边界必须落到确定性策略和操作系统执行层。',
    [['策略决策', '按用户、任务、工具参数和风险返回 allow、ask 或 deny。'], ['选择沙箱配置', '限制 filesystem、network、process、CPU、memory 和 wall time。'], ['管理进程树', '流式收集输出，支持超时、取消、后台 session 和分级终止。'], ['暴露真实边界', '平台降级或无沙箱 fallback 必须显式记录并阻止高风险动作。']],
    code('策略引擎检查工具调用', 'python', [
      'def authorize(call, context):',
      '    rule = policy.lookup(user=context.user, tool=call.name)',
      '    if not rule.allowed:',
      '        return Decision.deny(\"permission_denied\")',
      '    if call.estimated_impact > rule.auto_approve_limit:',
      '        return Decision.require_approval(',
      '            action=call, reason=\"high_impact_write\"',
      '        )',
      '    return Decision.allow(sandbox=rule.sandbox_profile)',
    ], ['策略判断应是确定性代码，并记录命中的 rule 版本。', 'Decision.allow 仍应携带 sandbox profile，授权不等于绕过隔离。', '进程取消要覆盖整个进程树，并记录是否清理成功。']),
  ),
  'harness-recovery': guide(
    '错误处理不是把同一个调用再跑三次，而是判断失败属于参数、权限、网络、资源、环境还是部分副作用，并选择修改、重试、降级、暂停或终止。',
    '像医院分诊：症状相同不代表处理相同，先分类和记录，再决定复查、转科或立即停止。',
    'Agent 长任务一定会遇到错误；没有结构化错误、幂等和 pending action，重试本身就可能成为更大的故障。',
    [['分类错误', '生成稳定 code、retryable、影响范围和可行动提示。'], ['选择恢复策略', '参数错误交回模型，瞬时错误退避，权限问题转审批，永久错误换方案。'], ['检测无进展', '比较状态 delta、调用签名和错误序列，识别循环与振荡。'], ['从崩溃恢复', '依据持久 intent、幂等结果与外部状态决定继续、补偿或人工接管。']],
    code('有边界的结构化重试', 'python', [
      'async def execute_with_recovery(call, run):',
      '    key = idempotency_key(run.id, call.id)',
      '    for attempt in range(policy.max_attempts(call.name)):',
      '        try:',
      '            return await executor.run(call, key=key, timeout=30)',
      '        except ToolError as error:',
      '            trace.record(error.code, attempt=attempt)',
      '            if not error.retryable:',
      '                return ToolResult.error(error.code, error.action_hint)',
      '            await backoff(attempt, retry_after=error.retry_after)',
      '    return ToolResult.error("retry_exhausted", "choose_another_plan")',
    ], ['自动重试只处理明确的瞬时错误。', '写操作在重试前查询幂等结果或外部状态。', 'retry_exhausted 是可供模型和终止器处理的 observation。']),
  ),
  'harness-termination': guide(
    '终止系统要把成功、预算耗尽、模型交回和人工升级分开；审批是允许一个明确动作，澄清则是补齐缺失信息。',
    '像列车调度：到站、燃料不足、司机请求指示和紧急停车都属于“停”，但后续处理完全不同。',
    '如果所有退出都只保存最终文本，系统就无法正确计分、恢复审批、清理资源或向用户解释为什么没有完成。',
    [['产生停止候选', '模型返回 final/no-tool、verifier 成功、预算触发或风险升级。'], ['分类退出原因', '记录 success、exhausted、handback、escalation、failed、cancelled 或 paused。'], ['持久化等待状态', '保存 pending action/question、版本、预算、过期策略和恢复游标。'], ['恢复前重新检查', '验证用户授权、外部状态、幂等结果和资源租约。']],
    code('四类退出与等待状态', 'typescript', [
      'type Exit =',
      '  | { kind: "success"; evidence: string[] }',
      '  | { kind: "budget_exhausted"; dimension: string }',
      '  | { kind: "model_handback"; question: string }',
      '  | { kind: "human_escalation"; pendingAction: ToolCall };',
      '',
      'async function pause(run: Run, exit: Exit) {',
      '  await store.compareAndSet(run.version, {',
      '    status: "paused", exit, resumeCursor: run.lastEventId,',
      '  });',
      '}',
    ], ['模型 final 还需 verifier 接受才能进入 success。', '审批展示具体 pendingAction，而不是笼统的“继续”。', 'Resume 使用 compare-and-set 避免重复点击推进两次。']),
  ),
  'harness-orchestration': guide(
    '子 Agent 的核心不是多一个角色提示，而是拥有独立上下文、工具投影、预算和状态，并通过明确协议把结果交回父 Agent。',
    '像项目组：真正的分工需要独立工作区、权限和交付格式，不是所有人坐在一个会议室里换名字说话。',
    'Context isolation 能减少污染并控制权限，但也会丢失隐含信息；编排必须显式设计输入、结果、并发、失败和聚合。',
    [['定义隔离边界', '按任务、数据权限或工具能力创建 child context，不按虚构人格拆分。'], ['选择编排者', '固定依赖由代码控制，开放分解才交给模型。'], ['用事件回传', '结果包含 task id、status、provenance、artifact、cost 和 schema。'], ['限制并发与递归', '设置 fan-out、深度、共享资源 ownership 和部分失败策略。']],
    code('带隔离合同的子 Agent', 'typescript', [
      'const child = await orchestration.spawn({',
      '  task: subtask,',
      '  context: selectMinimumContext(parentState, subtask),',
      '  tools: registry.project({ role: "researcher", readOnly: true }),',
      '  budget: { turns: 6, tokens: 12_000 },',
      '  outputSchema: ResearchResult,',
      '});',
      '',
      'const event = await mailbox.wait(child.id);',
      'parentState.add(validateChildResult(event));',
    ], ['角色名不提供隔离，独立 context/tool/state 才提供。', '父 Agent 验证结果来源和 schema，不能把子 Agent 文本当事实。', '共享写资源需要 ownership、分支或锁。']),
  ),
  'harness-observability': guide(
    'Tracing 是把一次 Agent 运行拆成可追踪的 span：模型调用、工具调用、handoff、审批和验证都留下时间线。',
    '像快递物流轨迹：只知道最终没送到无法排查，必须知道卡在分拣、运输还是签收。',
    '没有结构化 trace，失败只能靠聊天文本猜；有了 trace 才能 replay、分层评测和定位模型/工具/Harness 责任。',
    [['定义 span schema', '统一 run、turn、model、tool、policy、approval 和 verifier 事件。'], ['关联版本', '记录模型、prompt、tool schema、环境和策略版本。'], ['捕获结果与成本', '保存状态码、延迟、token、重试和脱敏摘要。'], ['离线 replay', '用固定 trace 替换某一组件，比较失败是否消失。']],
    code('结构化 Tool Span', 'json', [
      '{',
      '  \"trace_id\": \"tr_42\",',
      '  \"span_id\": \"sp_tool_3\",',
      '  \"parent_span_id\": \"sp_model_2\",',
      '  \"kind\": \"tool_call\",',
      '  \"tool\": \"get_order@v3\",',
      '  \"policy_decision\": \"allow\",',
      '  \"latency_ms\": 184,',
      '  \"status\": \"ok\",',
      '  \"input_hash\": \"sha256:...\",',
      '  \"output_summary\": {\"status\": \"shipped\"}',
      '}',
    ], ['敏感输入优先保存哈希、结构和脱敏摘要，不要无条件记录原文。', 'parent_span_id 让模型决策与工具执行形成因果链。', 'Replay 必须标注哪些外部结果被模拟，不能伪装成真实线上执行。']),
  ),

  'agentic-harness-boundary': guide(
    'Agent 成功率低不自动意味着要做 RL：先判断是模型不会决策、Harness 没给对上下文/工具，还是环境与 verifier 本身有问题。',
    '像赛车调校：车手策略、赛车机械和赛道计时器要分开检查；同时改三者，成绩变好也不知道原因。',
    'Agentic RL 成本高且会放大 reward 与环境漏洞。只有稳定坐标系下仍存在探索瓶颈，训练 policy 才有清晰价值。',
    [['固定任务与环境', '锁定 fixture、verifier、工具和终止语义。'], ['建立强 Harness baseline', '先修 context、schema、错误、权限和状态机问题。'], ['比较固定坐标轴', '固定模型做 Harness A/B，固定 Harness 比较 SFT/RL policy。'], ['检查交互效应', '用二乘二消融同时报告成功、成本、延迟、安全与轨迹。']],
    code('Policy × Harness 二维消融', 'python', [
      'for policy in [sft_policy, rl_policy]:',
      '    for harness in [baseline_harness, improved_harness]:',
      '        result = evaluate(',
      '            policy=policy, harness=harness, tasks=frozen_tasks,',
      '            budgets={"tokens": 20_000, "tool_calls": 12},',
      '        )',
      '        report(policy.version, harness.version, result)',
    ], ['四个格子使用同一任务、工具、环境与推理预算。', '固定 Harness 的 policy 差异才接近训练增益。', '组合最优可以上线，但研究归因必须保留分轴结果。']),
  ),
  'agent-environment': guide(
    'Agentic RL 环境把真实任务变成可反复练习的世界：能 reset 到已知起点、执行 action、返回受控 observation，并由独立 verifier 判断结果。',
    '像飞行模拟器：既要接近真实驾驶舱，又要能快速复位、注入故障、并行训练且绝不能撞到真实飞机。',
    '环境是在线生成训练数据的机器；泄漏答案、状态污染或 verifier bug 会持续制造错误 reward，比单条脏样本更危险。',
    [['构建隔离 Reset', '用快照、容器、数据库副本或浏览器 profile 绑定 seed 和版本。'], ['定义 Step 契约', '验证 action，执行 transition，并返回有限且带来源的 observation。'], ['隐藏 Verifier', '依据最终状态、隐藏测试和安全规则评分，不向策略暴露 gold。'], ['分类终止与故障', '分开 terminated、truncated、invalid、unsafe 与 infrastructure_error。']],
    code('版本化训练环境', 'python', [
      'class VersionedAgentEnv:',
      '    def reset(self, task, seed):',
      '        self.world = snapshots.restore(task.fixture, seed=seed)',
      '        self.version = task.environment_version',
      '        return self.observe(), {"env_version": self.version}',
      '',
      '    def step(self, action):',
      '        validated = self.action_schema.parse(action)',
      '        result = self.sandbox.execute(validated, self.world)',
      '        score = self.hidden_verifier.evaluate(self.world, result)',
      '        return self.observe(), score.reward, score.terminated, self.budget.done, score.info',
    ], ['hidden_verifier 与策略上下文物理隔离。', 'Infrastructure error 不应直接记成策略失败。', '训练与评测 fixture 分离，并记录 env/verifier 版本。']),
    'harness-stack',
  ),
  'agent-mdp': guide(
    'POMDP 把 Agent 任务描述为：看见不完整观察，根据内部历史选动作，环境变化后再给新观察，直到结束。',
    '像在陌生城市找人：你看不到全城状态，只能根据地图、询问结果和已有记忆决定下一步。',
    '语言模型的上下文不是完整世界状态；工具结果也可能延迟或不可靠，因此状态估计和环境定义同样重要。',
    [['定义隐藏状态', '真实文件、网页、数据库和任务进度构成环境状态。'], ['定义观察', '只把工具返回、界面片段和消息暴露给策略。'], ['定义动作', '工具调用、文本回复、handoff 和停止都有明确 schema。'], ['定义终止与奖励', '成功、失败、超时和违规必须可判定并可重置。']],
    code('标准环境接口', 'python', [
      'class AgentEnv:',
      '    def reset(self, task, seed):',
      '        self.state = provision_isolated_world(task, seed)',
      '        return self.observe()',
      '',
      '    def step(self, action):',
      '        result = execute(action, self.state)',
      '        observation = self.observe()',
      '        reward = self.verifier.score(self.state, result)',
      '        terminated = self.verifier.success(self.state)',
      '        truncated = self.budget.exhausted()',
      '        return observation, reward, terminated, truncated, result.info',
    ], ['terminated 表示任务自然结束，truncated 表示预算截断，两者 bootstrap 语义不同。', 'reset 必须可复现且隔离，避免不同 rollout 相互污染。', 'info 保留可观测诊断，不应偷偷向策略泄漏隐藏答案。']),
    'trajectory-credit',
  ),
  'agent-credit': guide(
    '长程信用分配要判断：最终成功究竟来自哪次搜索、哪个工具参数、哪次纠错，而不是平均奖励所有 token。',
    '像团队项目最终成功，不能把奖金平均归因于每句聊天；要识别关键决策和无效消耗。',
    '终局 0/1 奖励方差高，过程 shaping 又可能教错目标，需要用 turn 边界、反事实和价值基线组合。',
    [['切分决策点', '按 model turn 或工具动作，而不是任意 token，建立可解释节点。'], ['计算回报', '把终局成功、过程里程碑和成本沿轨迹传播。'], ['加入基线', '用同任务多轨迹、value 或反事实结果估计相对贡献。'], ['验证因果性', '删除或替换某个动作 replay，检查结果是否真的变化。']],
    code('Turn 级优势与成本', 'python', [
      'def turn_returns(events, final_success, gamma=1.0):',
      '    rewards = []',
      '    for event in events:',
      '        milestone = event.get(\"verified_progress\", 0.0)',
      '        cost = 0.001 * event[\"tokens\"] + 0.01 * event[\"tool_calls\"]',
      '        rewards.append(milestone - cost)',
      '    rewards[-1] += float(final_success)',
      '    return discounted_returns(rewards, gamma)',
    ], ['成本系数会改变最优策略，必须和真实产品目标对齐。', 'verified_progress 应来自环境或独立 verifier，不让策略自报。', '对 shaping 做消融，确保最终成功率增益不是局部指标幻觉。']),
  ),
  'agent-systems': guide(
    '异步 Agentic RL 让很多环境 worker 持续采样，learner 同时更新模型，两边不必每批互相等待。',
    '像外卖骑手持续接单、调度中心不断更新路线；效率更高，但骑手手里的旧地图会逐渐过时。',
    '吞吐提升会带来 policy lag、重复轨迹、环境拥塞和版本混用，需要版本与队列治理。',
    [['分发任务与版本', '每条 rollout 绑定 prompt、environment 和 policy_version。'], ['并行执行环境', 'worker 隔离运行并持续上报 event 与 reward。'], ['过滤经验', 'learner 按版本差、完整性和质量门禁选择轨迹。'], ['同步新权重', '使用原子版本切换，旧 worker 在安全边界内完成或中止。']],
    code('带版本的 Experience Queue', 'python', [
      'async def rollout_worker(queue, policy_store, env):',
      '    policy, version = await policy_store.latest()',
      '    trajectory = await run_episode(policy, env)',
      '    await queue.put({\"policy_version\": version, \"trajectory\": trajectory})',
      '',
      'async def learner(queue, current_version, max_lag=2):',
      '    item = await queue.get()',
      '    if current_version - item[\"policy_version\"] > max_lag:',
      '        return \"drop_stale\"',
      '    return update_policy(item[\"trajectory\"])',
    ], ['丢弃陈旧经验会浪费算力，保留则增加偏差，需要用实验选择 max_lag。', '队列元素应有唯一 trajectory_id，防止重试导致重复训练。', '环境版本同样要记录，否则 reward 不可比较。']),
  ),

  'eval-stack': guide(
    '评测要从最便宜的单元检查一路走到真实任务：数据和函数正确、模型能力正确、系统链路正确、产品结果正确。',
    '像测试汽车：先测零件，再上试验台，再跑封闭道路，最后观察真实车队运营。',
    '只看一个总榜会掩盖具体退化，也无法告诉你该修数据、模型、工具还是产品流程。',
    [['L0 数据与单元', '检查模板、mask、verifier、tool schema 和确定性函数。'], ['L1 模型能力', '按能力桶做固定解码与污染审计。'], ['L2 系统与轨迹', '运行完整 Harness、工具和环境，评估成功与恢复。'], ['L3 在线业务', '在安全发布下观察用户结果、成本和长期副作用。']],
    code('分层评测清单', 'yaml', [
      'suite:',
      '  unit:',
      '    - chat_template_roundtrip',
      '    - verifier_adversarial_tests',
      '  model:',
      '    - factuality_holdout',
      '    - code_hidden_tests',
      '  system:',
      '    - tool_failure_recovery',
      '    - prompt_injection_scenarios',
      '  online:',
      '    - task_completion_rate',
      '    - human_escalation_rate',
    ], ['每项指标要有 owner、版本、样本来源和发布门槛。', '模型离线分数不能替代系统级任务成功率。', '失败样本应能回链到 trace 和组件版本。']),
    'eval-stack',
  ),
  'agent-eval': guide(
    'Agent 评测不仅看最终成没成功，还要看走了什么路径、工具是否用对、成本是否合理、失败能否恢复。',
    '像评估司机不能只看是否到达，还要看是否闯红灯、绕远路或需要乘客接管。',
    '只用最终成功率会让高成本暴力搜索看起来优秀，也会漏掉危险但碰巧成功的轨迹。',
    [['定义任务与环境', '固定初始状态、隐藏目标、扰动和重置规则。'], ['同时评 outcome 与 trace', '记录成功、关键动作、错误恢复、步数和成本。'], ['运行扰动集', '注入超时、空结果、冲突信息和权限拒绝。'], ['做组件归因', '重放相同轨迹，替换模型、工具或 Harness 判断失败来源。']],
    code('轨迹级评分', 'python', [
      'def score_trace(trace, task):',
      '    return {',
      '        \"success\": task.verifier(trace.final_state),',
      '        \"invalid_tool_calls\": sum(not x.valid for x in trace.tool_calls),',
      '        \"recovery_rate\": recovered_errors(trace) / max(1, total_errors(trace)),',
      '        \"steps\": len(trace.turns),',
      '        \"cost_usd\": trace.model_cost + trace.tool_cost,',
      '        \"policy_violations\": count_policy_violations(trace),',
      '    }',
    ], ['不要立即聚合成单分，先保存各维度分布。', 'success 与 policy_violations 应有硬门禁关系。', '按任务难度和失败类型分桶，平均值不足以指导改进。']),
  ),
  'safety-eval': guide(
    '安全评测是在模型和系统可能被诱导、误用或遇到恶意输入前，主动寻找越权、泄漏和危险行动。',
    '像消防演练：不是因为相信员工会小心就省掉门禁，而是预先验证多层保护是否真的生效。',
    '安全不是一个拒答率。过度拒答会伤害可用性，Agent 还要测试间接注入、工具越权和副作用。',
    [['建立威胁模型', '列出攻击者、资产、入口、权限与最坏影响。'], ['构造攻击矩阵', '覆盖直接/间接注入、数据外泄、越权工具和沙箱逃逸。'], ['测试多层防线', '分别验证模型、context、policy、tool 和环境层。'], ['保留可用性对照', '同场景加入合法请求，测量 false refusal 与任务成功。']],
    code('安全场景矩阵', 'python', [
      'scenarios = [',
      '    {\"attack\": \"indirect_injection\", \"tool\": \"read_only\", \"expected\": \"ignore_injected_instruction\"},',
      '    {\"attack\": \"cross_tenant_access\", \"tool\": \"search\", \"expected\": \"deny\"},',
      '    {\"attack\": \"destructive_write\", \"tool\": \"delete\", \"expected\": \"require_approval\"},',
      '    {\"attack\": \"none\", \"tool\": \"read_only\", \"expected\": \"complete\"},',
      ']',
      '',
      'results = [run_safety_case(agent, case) for case in scenarios]',
    ], ['合法对照样本防止通过“一律拒绝”虚假提高安全分。', '攻击成功要按真实影响分级，而不只计次数。', '红队样本、工具和 policy 版本必须冻结以便回归。']),
  ),
  'causal-eval': guide(
    '可归因评测要回答“提升真的是这次训练造成的吗”，而不是仅观察新模型分数更高。',
    '像药物实验要有对照组；同时换数据、模板和解码参数，就无法知道是哪项起作用。',
    '后训练差异可能来自随机 seed、评测污染、judge 漂移或采样噪声，需要消融和置信区间。',
    [['冻结对照', '相同 base、数据切分、推理参数和评测版本建立 baseline。'], ['单变量消融', '每次只加入一个数据源、loss 或系统机制。'], ['估计不确定性', '按 task bootstrap，报告差值区间而不只报点估计。'], ['审计污染', '做精确与语义近重复检查，隔离评测来源。']],
    code('配对 Bootstrap 差值', 'python', [
      'import numpy as np',
      '',
      'def paired_bootstrap(old_scores, new_scores, rounds=5000):',
      '    delta = np.asarray(new_scores) - np.asarray(old_scores)',
      '    means = []',
      '    for _ in range(rounds):',
      '        sample = np.random.choice(delta, size=len(delta), replace=True)',
      '        means.append(sample.mean())',
      '    return np.percentile(means, [2.5, 50, 97.5])',
    ], ['按任务配对能减少题目难度差异造成的方差。', '样本不是独立同分布时，应按来源或用户聚类 bootstrap。', '置信区间跨 0 时不应宣称稳定提升。']),
  ),
  'online-eval': guide(
    '在线评测是让新模型在真实流量旁边观察、少量服务，再逐步扩大，同时随时能回滚。',
    '像新版本先影子运行，再给内部用户，最后逐步放量，而不是一次替换全部生产。',
    '离线集无法覆盖真实分布漂移和人机交互，但在线实验也不能拿用户安全做探索。',
    [['Shadow', '复制请求但不影响用户，比较输出、成本和潜在风险。'], ['Canary', '给受控小流量真实服务，设硬回滚门槛。'], ['A/B', '随机分流并预注册主要指标、护栏指标和实验周期。'], ['持续监控', '检测流量、模型、工具和用户行为漂移。']],
    code('渐进发布门禁', 'yaml', [
      'release:',
      '  stages: [shadow, canary_1pct, canary_10pct, ab_50pct]',
      '  primary_metric: task_completion_rate',
      '  guardrails:',
      '    safety_incident_rate: \"<= baseline\"',
      '    p95_latency_ms: \"<= 2200\"',
      '    cost_per_success: \"<= baseline * 1.10\"',
      '  rollback:',
      '    automatic: true',
      '    restore_version: production_previous',
    ], ['门槛要在实验前确定，避免看到数据后挑指标。', '成本最好按成功任务归一化，而不只看单次请求。', '回滚同时覆盖模型、prompt、tool schema 和 policy 版本。']),
  ),

  'parallelism-memory': guide(
    '显存账本把“模型为什么放不下”拆成参数、梯度、优化器、激活和临时 buffer，再决定切哪一部分。',
    '像搬家先列出家具、衣物和纸箱体积，才知道需要拆家具、分车还是少带东西。',
    '并行策略不是越多越好；通信、重算和气泡会吃掉理论收益，应先解决真正的最大项。',
    [['测单卡基线', '记录每类显存、tokens/s、序列长度和 batch。'], ['选择切分维度', '数据、张量、流水线、序列和 ZeRO/FSDP 解决不同瓶颈。'], ['估算通信', '看带宽、跨节点比例、all-reduce 和权重同步。'], ['做端到端压测', '以有效 tokens/s/GPU 与总成本比较，而非只看是否 OOM。']],
    code('粗略显存账本', 'python', [
      'def memory_budget(params_b, bytes_per_param, activation_gb, world_size, zero_stage):',
      '    weights = params_b * bytes_per_param',
      '    gradients = params_b * 2',
      '    optimizer = params_b * 8',
      '    if zero_stage >= 3:',
      '        weights /= world_size',
      '    if zero_stage >= 2:',
      '        gradients /= world_size',
      '        optimizer /= world_size',
      '    return {\"weights_gb\": weights, \"gradients_gb\": gradients, \"optimizer_gb\": optimizer, \"activations_gb\": activation_gb}',
    ], ['示例是数量级模型，真实 dtype、量化、flatten buffer 和碎片会改变结果。', '激活取决于 batch、sequence、层数和 checkpointing，必须实测。', 'ZeRO/FSDP 降显存但增加通信，不能只看容量。']),
    'memory-budget',
  ),
  'rollout-inference': guide(
    'Rollout Engine 是专门负责快速生成训练轨迹的推理服务；它需要 KV Cache，并定期接收 learner 新权重。',
    '像训练场里一组模拟器持续出题和记录动作，教练更新策略后要把新战术同步给所有模拟器。',
    '训练框架和推理引擎对 tokenizer、logprob 与权重格式理解不一致，会让 ratio 和奖励悄悄出错。',
    [['准备推理副本', '按并发、序列长度和 KV Cache 预算放置 rollout worker。'], ['生成带版本轨迹', '保存 token、old logprob、采样参数和 policy version。'], ['同步权重', '全量、增量或共享内存更新后做一致性探针。'], ['验证 serving parity', '固定输入比较 trainer 与 rollout engine 的 token 和 logprob。']],
    code('权重同步后的探针', 'python', [
      'def sync_and_probe(learner, rollout_engine, prompts):',
      '    version = learner.export_weights()',
      '    rollout_engine.load_weights(version)',
      '    for prompt in prompts:',
      '        train_logp = learner.score(prompt)',
      '        serve_logp = rollout_engine.score(prompt)',
      '        assert max_abs_diff(train_logp, serve_logp) < 1e-4',
      '    rollout_engine.activate(version)',
    ], ['先探针再原子激活，避免半更新权重提供 rollout。', '容差取决于精度和 kernel，不应盲目复制 1e-4。', 'tokenizer、chat template 和 logits processor 也要随版本同步。']),
  ),
  'async-architecture': guide(
    '同步训练每轮等所有 rollout 完成再更新；异步训练让快 worker 不等慢 worker，但经验会逐渐陈旧。',
    '像课堂统一收卷后讲评与学生随做随交、老师持续批改：后者吞吐高，版本一致性更难。',
    '系统设计的核心不是追求最高 GPU 利用率，而是在吞吐、policy lag、经验质量和实现复杂度间找平衡。',
    [['测等待来源', '区分生成长尾、环境慢调用、打分和权重同步。'], ['选择同步粒度', '全同步、微批流式或完全异步对应不同偏差。'], ['治理队列', '设置 backpressure、去重、优先级和最大版本差。'], ['评估净收益', '同时比较 wall-clock 收敛、样本效率和最终质量。']],
    code('带 Backpressure 的队列', 'python', [
      'class ExperienceBuffer:',
      '    def __init__(self, max_size):',
      '        self.queue = AsyncQueue(maxsize=max_size)',
      '',
      '    async def put(self, item):',
      '        await self.queue.put(item)  # full 时阻塞生产者',
      '',
      '    async def get_valid(self, current_version, max_lag):',
      '        while True:',
      '            item = await self.queue.get()',
      '            if current_version - item.policy_version <= max_lag:',
      '                return item',
    ], ['Backpressure 防止 rollout 无限堆积并变陈旧。', '丢弃原因和浪费 token 要进入成本指标。', '完全异步算法还需处理 off-policy 校正，队列过滤不是全部。']),
  ),
  'rl-observability': guide(
    'RL 可观测性要让你知道每一分奖励、每次更新和每条轨迹来自哪个模型与环境版本，以及成本花在哪里。',
    '像工厂看板不仅显示产量，还显示哪条生产线、哪批原料、多少返工和停机时间。',
    '只监控总 reward 和 GPU 利用率无法定位 reward hacking、数据陈旧、环境故障和吞吐浪费。',
    [['建立统一 ID', 'run、batch、prompt、trajectory、policy 和 environment 都有可关联标识。'], ['分层指标', '算法、数据、系统、环境和业务指标分别展示。'], ['故障可恢复', 'checkpoint、队列和环境状态支持幂等重放。'], ['成本归因', '按成功样本统计生成、打分、训练、失败和丢弃成本。']],
    code('每条轨迹的可观测字段', 'json', [
      '{',
      '  \"trajectory_id\": \"traj_0182\",',
      '  \"policy_version\": 37,',
      '  \"environment_version\": \"code-env-12\",',
      '  \"prompt_bucket\": \"python_hard\",',
      '  \"reward_components\": {\"tests\": 1.0, \"format\": 0.2, \"cost\": -0.08},',
      '  \"tokens\": 1840,',
      '  \"rollout_ms\": 9120,',
      '  \"accepted_for_training\": true,',
      '  \"retries\": 1',
      '}',
    ], ['reward_components 必须保留，聚合分不足以诊断。', '失败和被丢弃轨迹同样要计成本。', '日志中的 prompt 与输出应按隐私策略脱敏或哈希。']),
  ),

  'rejection-sampling': guide(
    '拒绝采样是让模型一次写多个答案，用验证器或 Judge 选出较好的，再把选中答案当成新教材。',
    '像同一篇作文写五稿，挑最好的一稿归档；它用更多生成成本换来更干净的监督数据。',
    '提升来自生成覆盖和选择器两部分，若 Judge 选错或只偏爱长答案，错误会被蒸馏进学生。',
    [['生成多样候选', '固定来源策略和采样参数，记录候选数 N。'], ['独立验证与排序', '程序 verifier 优先，开放任务使用校准 judge。'], ['控制多样与难度', '去除近重复，不让数据只剩单一风格或简单题。'], ['回灌并复测', '作为 SFT target 或偏好 pair，重新评估 pass@1。']],
    code('Best-of-N 数据生产', 'python', [
      'def best_of_n(policy, task, n, selector):',
      '    candidates = [policy.generate(task.prompt) for _ in range(n)]',
      '    scored = [(candidate, selector.score(task, candidate)) for candidate in candidates]',
      '    winner, score = max(scored, key=lambda item: item[1])',
      '    return {',
      '        \"prompt\": task.prompt, \"chosen\": winner.text,',
      '        \"score\": score, \"n\": n, \"policy_version\": policy.version,',
      '    }',
    ], ['保存所有候选和分数，才能区分 proposal 与 selector 的问题。', '训练时按 N 或来源分桶，避免高 N 样本拥有隐藏优势。', '最终价值看蒸馏后 N=1 能力，不只看选优后的结果。']),
  ),
  'knowledge-distillation': guide(
    '蒸馏让学生模仿教师的概率分布或生成轨迹；软概率告诉学生“除了正确词，其他词有多不合适”。',
    '像老师不只给标准答案，还告诉你第二可能答案和常见误区，信息比一个对错标签更丰富。',
    'Logit KD 信息密但存储昂贵，Sequence KD 工程简单但只看教师采样出来的少量轨迹。',
    [['选择教师信号', '能访问 logits 用 soft targets，闭源教师用生成序列和验证器。'], ['调节温度', '温度升高让小概率候选更可见，并用 T² 调整梯度尺度。'], ['混合硬标签', 'KD loss 与真实标签 NLL 共同稳定训练。'], ['验证能力迁移', '用新难题、短预算和跨格式测试，而不只比较文风。']],
    code('温度蒸馏损失', 'python', [
      'import torch.nn.functional as F',
      '',
      'def distillation_loss(student_logits, teacher_logits, labels, temperature=2.0, alpha=0.7):',
      '    teacher_prob = F.softmax(teacher_logits / temperature, dim=-1)',
      '    student_log_prob = F.log_softmax(student_logits / temperature, dim=-1)',
      '    soft_loss = F.kl_div(student_log_prob, teacher_prob, reduction=\"batchmean\") * temperature**2',
      '    hard_loss = F.cross_entropy(student_logits.flatten(0, 1), labels.flatten(), ignore_index=-100)',
      '    return alpha * soft_loss + (1 - alpha) * hard_loss',
    ], ['teacher logits 应 detach 或在 no_grad 中计算。', 'padding 与 prompt mask 必须同时用于 soft/hard loss。', '词表和 tokenizer 不一致时不能直接逐 token 对齐 logits。']),
    'distillation',
  ),
  'onpolicy-distillation': guide(
    'On-policy 蒸馏先让学生走到自己真实会遇到的前缀和错误状态，再请教师在这些位置示范或给分布。',
    '像老师不是只讲标准教材，而是先看学生当前做错哪一步，再针对这个错误继续辅导。',
    '固定教师数据会逐渐偏离学生分布；在线教学覆盖更准，但引入教师调用、采样和版本同步成本。',
    [['学生生成状态', '从当前 student 采样前缀、完整回答或 Agent 轨迹。'], ['教师条件化指导', '教师对学生状态给 logits、修订或下一步动作。'], ['筛选有效监督', '过滤教师不确定、不可验证或违反约束的样本。'], ['混合离线锚点', '用固定高质量数据防止在线分布变窄和遗忘。']],
    code('学生状态上的教师续写', 'python', [
      'for prompts in loader:',
      '    with no_grad():',
      '        student_prefixes = student.generate(prompts, max_new_tokens=128)',
      '        teacher_targets = teacher.complete(student_prefixes)',
      '    batch = build_completion_only_batch(student_prefixes, teacher_targets)',
      '    loss = student.nll(batch, assistant_only=True)',
      '    loss.backward()',
      '    optimizer.step()',
    ], ['student_prefixes 是当前学生分布的关键，不应全部替换为教师重写。', '教师输出仍需 verifier 或质量过滤。', '每轮在线数据要记录 student/teacher 版本，避免不可归因。']),
  ),
  'compression-delivery': guide(
    '模型交付要把训练权重、Tokenizer、模板、采样配置和工具协议一起变成可部署且可回归的版本。',
    '像软件发布不只复制可执行文件，还要带依赖、配置、迁移脚本和测试报告。',
    '量化、LoRA 合并或服务引擎差异都可能抹掉训练增益，所以最终评测必须针对真实服务产物。',
    [['冻结交付清单', '权重、adapter、tokenizer、template、generation 和 license 一起版本化。'], ['执行压缩与合并', '选择量化校准集，验证 merge 前后 logits 与行为。'], ['检查 Serving Parity', '对齐特殊 token、stop、sampling、tool schema 和 logits processor。'], ['重跑发布门禁', '在最终引擎上执行质量、安全、延迟和成本评测。']],
    code('部署前一致性探针', 'python', [
      'def serving_parity_check(reference, deployed, prompts):',
      '    report = []',
      '    for prompt in prompts:',
      '        ref = reference.generate(prompt, temperature=0, max_tokens=64)',
      '        out = deployed.generate(prompt, temperature=0, max_tokens=64)',
      '        report.append({',
      '            \"same_tokens\": ref.token_ids == out.token_ids,',
      '            \"max_logit_diff\": max_abs_diff(ref.logits, out.logits),',
      '            \"same_stop_reason\": ref.stop_reason == out.stop_reason,',
      '        })',
      '    return report',
    ], ['量化后 token 不一定完全一致，应按任务设行为容差。', '探针覆盖普通对话、工具 JSON、长上下文和安全边界。', '最终发布 artifact 保存量化工具、校准集与服务镜像版本。']),
  ),

  decision: guide(
    '方法选型先问当前瓶颈是什么、手里有什么反馈、是否需要在线探索，而不是先选一个热门缩写。',
    '像看病先诊断再开药；SFT、DPO、PPO 和 GRPO 是不同适应症与成本的治疗方案。',
    '最复杂算法不等于最大净收益。高质量数据、验证器和评测不成熟时，复杂 RL 只会放大不确定性。',
    [['定义目标差距', '用分桶评测描述模型具体失败，不用“整体不够好”。'], ['盘点反馈', '示范、pair、可验证 reward 和主观 Judge 对应不同起点。'], ['判断探索需求', '离线候选是否覆盖可能的更优策略，任务是否为多步环境。'], ['跑最小升级实验', '从 Prompt/SFT 基线逐层升级，只有净增益稳定才增加系统复杂度。']],
    code('可解释的选型规则', 'python', [
      'def choose_method(feedback, needs_exploration, multi_turn):',
      '    if multi_turn and needs_exploration:',
      '        return \"Agent SFT baseline -> Agentic RL\"',
      '    if feedback == \"demonstrations\":',
      '        return \"SFT\"',
      '    if feedback == \"preference_pairs\" and not needs_exploration:',
      '        return \"DPO\"',
      '    if feedback == \"verifiable\" and needs_exploration:',
      '        return \"GRPO / RLVR\"',
      '    if feedback == \"subjective\" and needs_exploration:',
      '        return \"RM + PPO\"',
      '    return \"improve data and evaluation first\"',
    ], ['规则输出的是首个实验，不是永远的最终架构。', '每次升级都要与前一层强基线按质量、成本和风险比较。', '多步 Agent 先把 Harness 和环境做好，再谈 Agentic RL。']),
  ),
  'research-vs-industry': guide(
    '学术实验要证明一个机制在受控条件下有效，工业项目要证明整条链在真实约束下长期创造净价值。',
    '像赛车实验追求圈速机制，量产车还要考虑可靠性、维修、法规、油耗和驾驶者差异。',
    '论文榜单提升可以是选型证据之一，但不能替代数据许可、线上延迟、安全、维护和回滚评估。',
    [['复现核心结论', '确认论文设定、数据、基线和实现差异。'], ['映射业务条件', '找出真实流量、模型大小、工具和成本与论文不同之处。'], ['设计强基线', 'Prompt、SFT、rerank 或规则系统往往是必要对照。'], ['报告净价值', '质量增益减去训练、推理、标注、风险和维护成本。']],
    code('实验提案模板', 'yaml', [
      'hypothesis: \"GRPO improves hidden-test pass@1 over SFT\"',
      'change: \"GRPO with exact verifier\"',
      'control: \"same SFT checkpoint and rollout budget\"',
      'primary_metric: pass_at_1',
      'guardrails: [general_ability, safety, output_length]',
      'costs: [gpu_hours, verifier_runtime, engineer_days]',
      'stop_condition: \"CI crosses zero after planned sample size\"',
      'rollback_artifact: sft_checkpoint_v7',
    ], ['先写假设与停止条件，再看结果，减少事后挑选。', '成本和护栏指标与质量指标同等进入结论。', '保存失败分布和负结果，它们比单个平均分更能指导生产决策。']),
  ),
  roadmap: guide(
    '90 天路线的目标不是把所有框架都跑一遍，而是完成一组从简单到复杂、可复现并能解释失败的实验。',
    '像学游泳先掌握换气和短距离，再练长距离与开放水域；每一步都建立在前一步稳定能力上。',
    '没有固定评测和阶段交付物，学习很容易变成追论文、换框架和堆 notebook。',
    [['第 1 月打基础', '完成数据审计、SFT/LoRA、模板测试和分桶评测。'], ['第 2 月做偏好与推理', '在同一 base 上比较 DPO、拒绝采样和小型 GRPO。'], ['第 3 月进入 Agent', '构建可重置环境、Harness、Agent SFT 与轨迹评测。'], ['形成作品集', '发布配置、曲线、成本、失败案例和复现实验说明。']],
    code('项目里程碑', 'yaml', [
      'days_01_30:',
      '  deliverable: reproducible_sft_baseline',
      '  evidence: [data_manifest, template_tests, eval_report]',
      'days_31_60:',
      '  deliverable: preference_and_rl_ablation',
      '  evidence: [dpo_vs_sft, grpo_small_scale, cost_report]',
      'days_61_90:',
      '  deliverable: agentic_environment_project',
      '  evidence: [harness_trace, perturbation_eval, failure_analysis]',
    ], ['每阶段只有一个主交付物，避免同时开太多训练线。', 'evidence 必须能被别人复现和质疑。', '框架可以替换，但数据契约、评测和实验记录应保持稳定。']),
  ),
}
